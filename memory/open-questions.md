# 待确认项（阻塞决策的未知）

> 每条：问题 / 为什么阻塞 / 如何解 / 状态。解决后移入 directions.md 或 SCORES.md。

## ✅ Q1. dev ref 是否含 overlap 标注？—— 已答：含，且密集
- **答案**：83/106 session 有跨说话人 overlap（464 对）。`reference_self_overlap=0` 是被 collar=5 折叠的假象。
- **影响**：多人+overlap 是 fun-asr 聚类范式硬伤 → 支持 D3 换本地 diarization。

## ✅ Q2. 26% 里文本 vs 说话人各占多少？—— 已答：说话人占一半
- **答案**：纯文本 tcpWER=12.89%，说话人对齐吃掉 13.12 点；oracle 上界 15.34%。根因=fun-asr 系统性少估说话人数（48/106 匹配，少估 42）。
- **结论**：主攻说话人分离（D3），文本（D1）降级。详见 SCORES v001 诊断。

## Q3. 云端 API 调 Qwen 后处理是否违规？
- **为什么阻塞**：决定 X3（LLM 后处理）能否解锁。
- **如何解**：向组委会书面确认 FAQ#7 边界。
- **状态**：未确认，暂锁。优先级最低。

## Q4. 官方线上提交频率限制？
- **为什么阻塞**：决定提交策略（能否多刷 vs 珍惜每次）。见 SCORES.md 停止边界。
- **如何解**：查赛题页/规则。
- **状态**：未确认，暂按"每次都稀缺"对待。

## 🔴 Q5. v018 的生成脚本在哪？——**当前最优版本不可复现**（2026-08-04 提出，最高优先级）
- **问题**：v018 是**当前线上最佳**（0.1634，见 SCORES.md），但**生成它的代码在仓库里根本不存在**。
  已核实：无 v018 commit；**08-04 当天 `baseline/src/*.py` 没有任何文件被修改**
  （最新改动是 08-03 22:37 的 `simulate_conv.py`）；全仓除产物本身外零处提到 v018。
  三件产物（`baseline/submission_v018.json`、`baseline/output_test_v018/`、
  `submit/archive/submission_v018_20260804.json`）**截至本次补录前全部 untracked** ——
  补录 commit 已把**产物**补进 git，**但产物入库不等于可复现：缺的是生成它们的脚本。**
- **为什么阻塞**：
  - **最优版本无法重建**。产物在手还能再提交，但**任何一次改进都无从做起** —— 改不动一个不存在的脚本。
  - **dev 评测也做不了**。没有脚本就跑不出 v018 口径的 dev 预测，`dev tcpWER = 未测` 这一栏被锁死。
  - **规则只能靠反推，而反推已被证明不足以定规则**：按 (CAM 人数, MOSS 人数) 分格统计，
    同一格子同时出现在触发与未触发两边 → 至少还有一个判据观测不到（详见 `logs/2026-08-04.md`）。
  - **这是第三次同型事故**：v001 代码丢失（commit 只改了 directions.md）、
    v007 脚本只活在 `/tmp`（2026-07-31 抢救入库）。
    **前两次丢的是历史版本，这次丢的是当前最优版本。**
- **如何解**：脚本若还在**未保存的编辑器缓冲 / `/tmp` 等临时文件 / 另一台机器（GPU 机）**上，
  **必须立刻落盘到 `baseline/src/` 并 commit**。验收照搬 v007 的抢救标准：
  复跑单个 session，与已落盘产物**逐字节一致**才算抢救成功。
  `/tmp` 一清、编辑器一关，就永久失去复现能力。
- **状态**：**未解决，阻塞后续一切基于 v018 的迭代。** 需要人来回答脚本在哪 ——
  agent 不应替答，也无法从仓库里找出不存在的东西。

## Q6. `submission_v017b.json` 与 `submission_v010.json` 逐字节相同 —— v017b 的改动描述可能记错了
- **问题**（2026-08-04 在 v018 核验时**附带**发现，非 v018 补录范围）：
  - `baseline/submission_v017b.json` 与 `baseline/submission_v010.json` **逐字节相同**
    （`shasum -a 256` 同为 `34d48178…`，同 818677 字节），
    但文档记它们是**两条不同的规则**：SCORES v010 =「MOSS + CAM++ 按段择优（R1a 单症状）」，
    v017b =「v011 + CAM++≥6人则换回 MOSS」。**不同规则不该产出完全相同的文件。**
  - `online_ledger.md` 称 v017b 相对 v011「test 仅 7 段触发换源」，
    **实测 v017b 与 v011 有 88 个 session 内容不同**，不是 7 段。
- **为什么阻塞**：若 v017b 实际提交的是 v010 的产物，那么「v017b = 0.16377」这个分数
  **归属的是哪条规则**就是错的 —— 而 v011 → v017b → v018 这条演进线上的判断都建立在它之上。
  **事实层可能有一处错误归因在持续传播。**
  好消息：v010 从未线上提交（ledger 无 v010 行），**没有白耗提交机会**。
- **如何解**：单独立项核查 —— 重跑 v017b 声称的规则并与归档产物比对；
  同时核对 v010 / v017b 的生成命令与各自的 `output_test_*` 来源目录。
- **状态**：**未处理，且刻意未改动 v017b 的任何既有记录**（超出 v018 补录范围；
  没查清就改事实层只会制造第二处错误）。建议在 Q5 之后、calibrate 之前处理，
  因为重分层会用到 v017b 这一版的归档件。

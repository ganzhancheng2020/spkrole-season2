# 方案归档与复盘 — 说话人角色分离挑战赛 赛季2

本文回答四件事：**问题是什么、方案怎么长出来的、最终每一步具体怎么做、留下了什么。**
部署与运行见 [README.md](README.md)。

| | |
|---|---|
| **最终成绩** | **tcpWER = 0.14309** |
| 起点 | 官方 fun-asr 直出 dev 26.01%；首次提交 0.19467 |
| 线上提交 | 46 次，17 次刷新纪录（逐次分数与 Δ 见 [README.md](README.md#results)）|
| 周期 | 2026-07-23 → 09-01 |

**一句话方法**：双分支 + 规则路由 + 声纹校验的说话人拆分。

**三个最关键的判断**：

1. 第一周就用逐词归因定方向 —— 说话人对齐吃掉一半以上误差，**主攻分离、文本降级**。
2. 中期把**测量系统本身**当成课题做（四道验收关卡），此前 dev 上的改进有大半兑现不到线上。
3. 最后的突破不是换模型，而是**换问法** —— 把「这场有几个人」换成「这两半是不是两个人」。

---

# 1. 问题与度量

## 1.1 tcpWER 的三条性质决定了后面一切

指标是 tcpWER（meeteval 0.4.3，`--collar 5`）。

> **本文的单位约定**：线上成绩写作小数（如 `0.14309`）；dev 成绩写作百分数（如 `16.81%`）。
> **「点」= dev tcpWER 的一个百分点**（例：「−0.7405 点」指 dev 从 16.81% 降到 16.07%）。
> 「线上/dev 点」的**迁移率**即：线上小数的改善量 ÷ dev 百分点的改善量。

三条性质贯穿全程：

1. **做说话人全局最优置换。** 「把某段贴错人」不是局部错误 —— 它会破坏整场映射，
   一处改动可能牵动全局。后来实测：段级「标签对不对」命中 73%，端到端却是负收益，
   因为**段级正确 ≠ 全局映射更优**。
2. **归错人要赔两次。** 一个词挂错人，在真说话人那边算 deletion，在错的人那边算 insertion。
   **所以修对一次是双份收益。** 这是最终方案单场能省几十个错误的原因。
3. **collar=5 让时间戳几乎免疫。** meeteval 对词时间做段内线性插值，5 秒容差把时间精度问题
   完全吸收。我们后来实测「格式轴（切分/时间）收益恰好为 0」，正是这条的推论。

**第 2 条是最后一步的全部立足点。**

## 1.2 起点诊断：分丢在哪（07-23 ~ 07-26）

官方基线 fun-asr（开 diarization）直出：dev **26.01%**。

没有急着换模型，先把说话人对齐拆开单独算：

| 口径 | tcpWER |
|---|---|
| 纯文本（不计说话人） | **12.89%** |
| 完整（含说话人对齐） | 26.01% |
| oracle 说话人时间轴上界 | 15.34% |

**说话人对齐吃掉 13.12 点，占总误差一半以上。**
根因是 fun-asr 系统性少估人数：106 场里只有 48 场人数对，少估 42 场。

→ **主攻说话人分离，文本降级。** 这个判断定了整个赛程的方向，事后看是对的。

---

# 2. 方案演进

## 2.1 换范式，双分支成形（07-29 ~ 08-06）

**当时的问题**：「ASR + 聚类」是两阶段范式，先切分再转写，切错了文本无从补救。

**做了什么**：转向端到端说话人归属转写（SAT）—— MOSS-Transcribe-Diarize 0.9B（Apache-2.0），
一次前向同时给出文本、时间戳与说话人。

**回执**：v007 = 0.16593，单次 −2.87 点，**全程最大跃迁**。

**学到什么**：MOSS 不是全面更优 —— 它在部分场次严重塌缩（把多人并成一两个），
而 CAM++ 在这些场反而更准。于是有了**双分支 + 按场路由**：

```
few_spk  = MOSS 人数 ≤ 2
low_turn = MOSS 轮换率 ≤ turn_rate
若 (few_spk 或 low_turn) 且 CAM 人数 ≥ 3  →  该场改用 CAM 分支
```

其中**轮换率 = 该场中「与上一段说话人不同」的段数 ÷ 总段数**（0~1），
它低说明模型把长段连成一片、很可能漏掉了说话人交替。`turn_rate` 是它的判定阈值，
最终取 **0.05**（见 3.2 Step 10）。

规则在 dev 上手工归纳，两个症状取并集（v011 = 0.16379）。随后补两条：
CAM 人数 ≥6 判为多估、换回 MOSS（规则 D）；CAM==5 且 MOSS≥4 判为轻度少判、换 CAM（规则 H）。

## 2.2 文本轴，及它的天花板（08-08 ~ 08-21）

**做了什么**：核心机制是**词级声学仲裁**。同一段音频，MOSS 与 FireRedASR2 各给一个转写；
按差异块对齐拆开，对最长的若干块枚举 2^k 种混合，用 FireRed 的 teacher-forcing 声学似然当裁判选最优。

**回执**：段级仲裁 v024 = 0.15903 → 词级 v026 = 0.15756 → 调参 v048 = 0.14625。

**学到什么（两条硬结论）**：

> **仲裁信号必须来自音频。** 16 次基于文本特征的选源尝试全部失败，唯一成功的是声学似然。
> 原因：候选本身就是各模型的 argmax，任何**文本层面**的判据都是模型已经用过的信息，
> 判官拿不到增量；只有回到波形才有新信息。

> **微调轴对文本完全平坦。** 五个微调变体的文本质量在 dev 上几无差别，
> 分数差异全部来自说话人归属的随机扰动。

**这一阶段最贵的一课是 v030**：dev 14.810%，线上 0.1737 —— **迁移率 −216%**。
根因是数据泄露：checkpoint 在 81 段 dev 上训练，而 dev 评测用全 106 段，
**dev 分数是记忆化的假象**。从此所有微调评测改用 holdout。

## 2.3 转折点：把「测量」本身当成课题（08-19 ~ 08-26）

**当时的问题**：到 v048（0.14625）时出现了更根本的麻烦 ——
**dev 上的改进越来越不能兑现到线上。**

dev 只有 106 场，而收益高度重尾：常常前 3 场就占掉 85% 的净收益。
106 场里恰好含 3 个灾难段，394 场的 test 没有同等密度。
**局部改进在 dev 上「看起来在赢」是常态，不是例外。**

**做了什么**：建立**四道验收关卡**，任一不过即不提交：

| 关卡 | 内容 | 校准它的线上回执 |
|---|---|---|
| 1 触发广度 | 改动影响 ≥ 8 场 | v049 只触发 3 场，点估计符号是错的，关卡拦对了 |
| 2 **重尾** | **整段剔除最幸运的 3 场后仍为收益** | v046 +0.02028 / v050 +0.01118 / v052 +0.00165 |
| 3 自助重采样 | P(更好) ≥ 80% | — |
| 4 符号一致 | 改善场数 > 恶化场数 | — |

**学到什么**：第 2 关的校准代价是实付的 —— 三次绕过它出货，分别退步
**+0.02028 / +0.01118 / +0.00165**。

两次越关都不是「忘了规则」，而是**为规则找到了看似合理的例外**：
第一次用「另一个检验说可以」覆盖，第二次用「在分支层过了」替代链路层。
两次预测误差几乎相同（**都是 +0.0038**）——
**理由的说服力与它的正确性无关，这是最值得记住的一条。**

由此定下纪律：

> 四关有否决权。新检验只能**追加**在四关之后（把标准变严），**不能替代**其中任何一关；
> 且必须在**与提交物同口径**的对象上过（完整链路、全量 session），中间层级过关不算数。

配套建立了 **dev 4 折交叉验证集**（每场由未见过它的模型产生）与
**嵌套验证**（阈值只在内层折里选）。后者的价值在最后一步得到验证：
同一配置全量选优 −1.0644，嵌套后 −0.7405，**两者之差就是选择性偏差**。

## 2.4 逆向拆解：主损失原来不在识别（08-24）

**换了个问法**：不问「怎么得分」，而是**逐词归因「分丢在哪」**。

| 丢在哪 | 词数 | 占 ref |
|---|---|---|
| ✅ 转出来且归对人 | 17209 | 88.5% |
| 🔴 **转出来但归错人** | **1269** | **6.5%** |
| 🔴 根本没转出来 | 969 | 5.0% |
| 🔴 凭空多出（insertion）| 977 | 5.0% |

> 分母统一是 ref 的 19447 词。前三项构成 ref 的完整分割（88.5 + 6.5 + 5.0 = 100%）；
> **「凭空多出」是 hyp 里 ref 没有的词，不占 ref 的份额**，所以四行相加会超过 100%。

**归错人（6.5%）比没听出来（5.0%）多 30%。**
而此前绝大部分精力都花在文本轴 —— **一直在抢较小的那 5.0%。**

顺带否掉一个很强的直觉：那 977 个「凭空多出」里 57.7% 是段内连续重复
（「我也我也不戴」「没有没有」），看着像自回归模型的重复循环。
但**四档去重全线大幅变差**（最保守一档 2 改善 / 20 恶化）——
**ref 照录了这些口语重复，删掉就是删对的词。**

→ 主攻方向转向**归属轴**。

## 2.5 归属轴的长期僵局（08-27 ~ 08-30）

归属轴有空间 —— 在当时的 dev 交叉验证集上，若把说话人**全部换成正确答案**、文本与切分不动，
tcpWER 还能再降 **3.574 个 dev 百分点**（这与 1.2 那个 15.34% 不是一回事：
那是赛程之初在 fun-asr 链路上测的，此处是 v048 一带在 MOSS 链路上重测的）。
但机制一个个失败：

| 机制 | 死因 |
|---|---|
| 段级重贴标签 | 663 个长段只有 14 个错段却承载 1.51 点 ⇒ 干预精度须 >97.9% |
| 开新说话人（似然阈值）| τ 全档单调有害：missed↓ 但 falarm↑、ins 翻倍 |
| 词级时间切分 | 即便给 oracle 说话人时间轴仍净亏 1.87 点（6.7% 的字在重叠语音区）|
| 学习路由 / 置信度门控 | 6 个选源信号全部挂关卡 |
| 解码时换段 | 三档剂量-响应全线退步 |

共同的墙是**触发精度**：能判断「这里缺人」，判断不了「哪些段是他的」。
**盈亏平衡精度是 58.3%，而所有可用信号的逐段精度都在 25~35%。**

期间也测清了路由轴的边界：把 CAM 段数从 132 降到 83 拿到 −0.00153（v065 = 0.14354），
但继续降到 50、7 反而变差 —— **这条轴有极小值，不是单调的。**

到收官前一天，八条轴全部封死，v065 = 0.14354 看起来就是终局。

## 2.6 最后一天的突破：声纹校验（08-31）

回头重看「少估说话人数」这个老问题，这次把它量化：

| 每场说话人数 | 2人 | 3人 | 4人 | 5人 | 均值 |
|---|---|---|---|---|---|
| dev 参考标注 | 9.4% | 27.4% | 35.8% | 21.7% | **3.868** |
| 我们的 test 输出（**当时的 v065**）| 16.8% | 40.9% | 25.1% | 15.2% | **3.442** |

> 表中只列 2~5 人（覆盖 dev 94.3% / test 98.0% 的场次），其余为 1 人或 6 人以上；
> 均值按全部场次计算。

**每场少 0.426 人**（3.868 − 3.442）；dev 链路上同样少 0.18~0.33 —— 同向、同量级，是稳定的系统性偏差。
结合 1.1 第 2 条（归错人赔两次），修对一次就是双份收益。

### 2.6.1 第一版：用 CAM++ 的划分去拆 —— 失败

让 CAM++ 当向导：某个 MOSS 说话人若在 CAM++ 时间轴上横跨 ≥2 个簇，就按簇拆开。
dev 上确实有收益，**但第 2 关全档不过** —— 剔掉最幸运的 3 场就翻正。

逐场归因给出一条干净的分界：

| | camN vs refN |
|---|---|
| 帮上忙的 6 场 | **全部 camN == refN** |
| 帮倒忙的 4 场 | **全部 camN > refN** |

**机制是对的，错的只是「CAM 数得准不准」。**

于是去找更好的人数估计器 —— 测了 7 个（CAM++ / NeMo / DOVER / ERes2NetV2 / MOSS 等），
**逐场命中率全部约 50% 且互相关**，DOVER 融合反而最差。这条路走不通。

### 2.6.2 第二版：换个问法 —— 成功

关键的转念：**不问「这场有几个人」，只问「这一次要拆的两半，是不是两个人」。**

对每个待拆的（speaker，少数簇），取两半分段的 CAM++ 段级声纹（192 维）平均向量算余弦，
**`sim < 0.50` 才真的拆**。效果立刻反转：

| 配置 | 触发 | dev Δ | **剔 top3 后** | P | 好/坏 | 四关 |
|---|---|---|---|---|---|---|
| 无声纹校验 | 12 | −0.648 | **+0.301 ❌** | 83.0% | 7/5 | ✓✗✓✓ |
| **加声纹校验** | **9** | **−0.982** | **−0.132 ✅** | **98.5%** | **7/2** | **✓✓✓✓** |

而且这不是一个刀刃点：**`max_sim ≤ 0.60` 的每一格都过第 2 关，`≥ 0.65` 的每一格都不过。**
余弦 0.60 是「两个人」与「把一个人劈成两半」的物理分界。

嵌套验证（阈值只在内层折选）给出诚实估计 **−0.7405 dev 点**，4 折中 3 折独立选中同一配置。

**v094b 线上 0.14309** —— 本赛季最后一个、也是唯一一个兑现的新机制。

### 2.6.3 同一天还买到一条新教训

同族的 v094a（`sim<0.60`，dev 上更优）**四关全过，线上却退步 +0.00087**。
事后查明：**dev 在 `sim∈[0.50,0.60)` 区间触发场数为零** ——
四关在 0.60 通过，只是因为 dev 上 0.60 和 0.50 命中同一批场次。
关卡不是判错了，是**没有样本可判**。

> **新纪律：关卡通过 ≠ 该参数值被验证过。**
> 必须另查「该档位在 dev 上是否真的触发过新样本」。
> 这是与「不可覆盖第 2 关」并列的**第二种失效模式**。

---

# 3. 最终方案

## 3.1 链路总览

```
                  data/extracted/test/test/wav   （394 个 wav，原始数据）
                                  │
      ┌───────────────────────────┼───────────────────────────┐
 ┌────▼─────┐              ┌──────▼──────┐             ┌──────▼──────┐
 │ MOSS 分支 │              │  CAM 分支    │             │ CAM++ 分离   │
 │ Step 2-6 │              │  Step 7-9   │             │   Step 1    │
 └────┬─────┘              └──────┬──────┘             └──────┬──────┘
      │ test_v051_moss            │ test_cam_wa                │ diar + emb
      │   (5513 条)               │  (3682 条)                 │
      └───────────┬───────────────┘                            │
                  ▼                                            │
        Step 10  pick_ensemble（--moss-max-turn-rate 0.05）     │
                  ▼                                            │
        Step 11  normalize_output                              │
                  ▼                                            │
        Step 12  merge_submit → submission_v065.json (0.14354) │
                  ▼                                            │
        Step 13  cam_split_verify ◀────────────────────────────┘
                  ▼
            submission_v094b.json   （最终，线上 0.14309）
```

> **两分支的条数为什么不相加**：两条分支各自覆盖全部 394 场（MOSS 5513 条、CAM 3682 条），
> `pick_ensemble` 是**逐场二选一**，不是拼接 —— 最终取 311 场的 MOSS 结果 + 83 场的 CAM 结果，
> 合计 5186 条。

一键跑通：`bash run_full_pipeline.sh`；每一步的完整命令与参数见 [README.md](README.md#reproduce)。

## 3.2 逐阶段说明

**Step 1 — CAM++ 分离与段级声纹。** 对 394 段音频做谱聚类说话人分离，
说话人数限定在 3~7 人、同说话人合并阈值 0.70；同一次运行顺带缓存每个窗口的
192 维声纹向量，供后面 Step 13 复用，不必二次提特征。产出分离时间轴与声纹两份缓存。

### MOSS 分支（最终被路由采用 311/394 场）

**Step 2 — MOSS-SAT 解码。** 用仿真数据微调过的 MOSS-Transcribe-Diarize 0.9B
（微调配方见 3.5）对整段音频做端到端解码，一次前向同时给出切分、时间戳与初始说话人标签，
得到 5513 条记录。这是整条链路的骨架，后面所有步骤都只在它上面做局部修正。

**Step 3 — FireRed 重转写。** 保持 MOSS 的切分不动，用 FireRedASR2 对同样的音频区间
重新转写一遍，为每一段拿到第二个独立的文本候选。

**Step 4 — 词级声学仲裁。** 把两个候选按差异块对齐拆开，对最长的 6 个差异块枚举
2⁶ 种 MOSS/FireRed 混合，用 FireRed 的 teacher-forcing 声学似然当裁判，
净胜出超过 0.05 才替换。相对 Step 2 改动文本 1169 条，**说话人标签一条不动**。

**Step 5 — 归属改写。** 在段内切多个窗取声纹，用留一法（扣掉本段自己的窗）
检查每段的说话人归属是否与该说话人的整体声纹一致，一致性低于 0.55 且挑战者赢过 0.05
才改判。改 26 条说话人标签，**文本与时间戳逐条不变**。

**Step 6 — 短段归属改写。** 短段的声纹不可靠，改用 MOSS 自己的说话人标记似然来判，
只处理词数 ≤5 的段、且替代者要超出当前者 1.5 才换。改 47 条标签，MOSS 分支到此完成。

### CAM 分支（最终被路由采用 83/394 场）

**Step 7 — fun-asr 转写。** 开启 diarization，由 fun-asr 原生给出分段与说话人，
得到 3682 条记录。这条分支的文本弱于 MOSS，但说话人边界更贴合。

**Step 8 — 用 CAM++ 声纹重打标签。** 保留 fun-asr 的分段与文本，
只用 CAM++ 声纹重新判定每段属于谁。改 87 条标签，**文本逐条不变**。

**Step 9 — CAM 分支的词级声学仲裁。** 配方与 Step 4 完全相同，
把这条分支的文本也过一遍声学裁判。改 828 条文本，**说话人不变**，CAM 分支到此完成。

### 合流与最终产物

**Step 10 — 逐场路由。** 按 2.1 的规则在两分支间逐场二选一：
MOSS 判出的说话人 ≤2 人、或轮换率 ≤0.05，且 CAM 判出 ≥3 人时改用 CAM 分支；
另有两条修正规则（CAM ≥6 人视为多估、换回 MOSS；CAM 恰为 5 人且 MOSS ≥4 人视为轻度少判、换 CAM）。
最终 83 场走 CAM、311 场走 MOSS。

**Step 11 — 字符规范化。** 把参考标注词表中出现 0 次的字符改写成参考使用的写法
（如「哦/喔」→「噢」、「哎/诶」→「唉」、「您」→「你」、阿拉伯数字拆成中文单字）。
共改 108 个字符，涉及 85 场。

**Step 12 — 合并。** 按 `(session_id, start_time)` 排序合并为单个 SegLST，
得到 `submission_v065.json`（线上 0.14354）。

**Step 13 — CAM 引导 + 声纹校验的说话人拆分。** 本方案的关键增量，机制见 2.6：
当 CAM++ 判出的人数比当前多 1 人、且某个说话人的分段横跨两个 CAM++ 簇时，
取两半分段的平均声纹算余弦，**只有低于 0.50 才真的拆开**。
最终触发 14 场、改 32 条记录，得到 `submission_v094b.json`（线上 0.14309）。

## 3.3 各环节值多少分（线上实测）

每次消融都要花掉一格线上额度（每天 3 次），额度全部用在了往前推进上，
所以**没有做「从最终链路逐个移除」的完整消融**。但演进过程中有若干版本本身就是单变量对照，
下表把它们整理出来 —— 全部是**线上回执**，不是 dev 估计：

| 对应 Step | 机制 | 线上证据 | Δ | 对照类型 |
|---|---|---|---|---|
| Step 2 | MOSS 端到端 SAT（换掉 ASR+聚类范式）| v002 → v007 | **−0.02874** | 引入时 |
| Step 10 | 双分支按场路由 | v007 → v011 | −0.00214 | 引入时 |
| Step 4 | 声学仲裁：段级 → 词级 | v024 → v026 | −0.00147 | 单变量（改粒度）|
| Step 11 | 输出层字符规范化 | v026 → v028 | −0.00183 | 引入时 |
| Step 4 | word_arb 参数 `MARGIN` 0→0.05、`MAX_BLOCKS` 4→6 | v048 | −0.00058 | 单变量（改参数）|
| **Step 5** | **`spk_reassign`（归属改写）** | **v049 = 从链路中移除它** | **+0.00112** | **真·消融** ✅ |
| Step 9 | CAM 分支首次做声学仲裁 | v051 | −0.00118 | 引入时 |
| Step 10 | 路由阈值 `turn_rate` 0.08 → 0.05 | v065 | −0.00089 | 单变量（改参数）|
| Step 10 | 路由再放宽（82 → 92 场走 CAM）| v078 | **±0.00000** | 单变量 —— 该轴已饱和 |
| Step 13 | CAM 引导 + 声纹校验的说话人拆分 | v065 → v094b | −0.00045 | 引入时 |

**读法要注意**：除 v049 外，其余都是「**引入这一环时**相对当时链路的增益」，
不等于「**从最终链路移除它**会退步多少」—— 后者会因下游补偿而偏小。
唯一做过真消融的是 Step 5：把它从链路里拿掉，线上退步 **+0.00112**。

v078 那行值得单独看：路由从 82 场放宽到 92 场，线上**一位不差**。
这说明路由轴在 v065 处已经到顶，后续所有收益必须来自别处 —— 这也是最后转向归属轴的直接原因。

## 3.4 谱系证据

各阶段的父子关系**由产物内容逐条比对实证确定**，不是靠日志断言 ——
这也是确认「哪个目录喂给哪个阶段」的依据。`run_full_pipeline.sh` 会自动校验这 5 条边：

| 比对 | 条数 | 文本+时间 | 说话人 | 结论 |
|---|---|---|---|---|
| `test_raw` → `test_wa_m05b6` | 5513 = 5513 | 差 1169 条 | 差 **0** | Step 4 只改文本 ✓ |
| `test_wa_m05b6` → `test_v048_re` | 5513 = 5513 | **差 0** | 差 26 | Step 5 只改标签 ✓ |
| `test_v048_re` → `test_v051_moss` | 5513 = 5513 | **差 0** | 差 47 | Step 6 只改标签 ✓ |
| `test_cam_multi` → `test_cam_re` | 3682 = 3682 | **差 0** | 差 87 | Step 8 只改标签 ✓ |
| `test_cam_re` → `test_cam_wa` | 3682 = 3682 | 差 828 条 | 差 **0** | Step 9 只改文本 ✓ |

**跨机器复现验证**：把仓库产物与脚本搬到另一台机器（NVIDIA RTX 3090 / Linux）重跑合流阶段，
`submission_v065.json` 与最终的 `submission_v094b.json` 两个 SHA256 **均与提交件逐字节一致**；
短段归属改写（Step 6）的输出也与归档逐条相同。

> 依赖声纹模型的两步（Step 1 分离与声纹、Step 8 重打标签）对 `modelscope` 版本敏感 ——
> 换用不同版本会得到不同的声纹向量。因此 [README.md](README.md#software) 的依赖表钉住了
> 具体版本，仓库同时归档了这两步的产物，复现时直接取用即可。

**v065 的两条分支来源**：`test_v051_moss`（311 场被采用）+ `test_cam_wa`（83 场被采用），
已逐字节鉴定确认。

## 3.5 MOSS 微调（Step 2 所用模型的来源）

用官方 dev 的**全部 106 场**切片重组成 800 条仿真对话作为微调素材（不引入任何外部语料），
在 MOSS 基座上训 2 个 epoch，学习率 1e-5，优化器 AdamW。生成与训练的完整命令见
[README.md](README.md#reproduce) 与 `run_full_pipeline.sh`。

**仿真数据可确定性重生成，因此不随仓库分发（1.0 G）。** 生成脚本用单一
`random.Random(seed)` 驱动全部采样，输入只有 `data/extracted/dev/dev/` 下的参考标注与音频
（都在仓库内），生成约 45 秒。这一点是**对着训练出货模型的那份真实数据验证过的**：
原始数据归档在 GPU 机 `/root/autodl-tmp/ft/sim_all106`（800 条），本地重跑后
`ref.seglst.json` 的 SHA256 与归档**逐字节一致**，抽样的 5 个音频也**全部一致**，
生成日志的时长画像（中位 42.7 s，dev 41.9 s）与当时的记录相同。

**配方是怎么定的**：先用 82 场素材重组 800 条，拿到 0.15096；把数据加到 3000 条反而退步到
0.15879 —— 说明**多样性在 800 条已饱和，再抽只是让模型反复听同一批人**。
改为用全 106 场作素材（源说话人 318 → 410，+29%）后拿到 0.14683。
结论：**加人有效，加 clip 有害。**

仓库内另存有三份历史仿真数据集：`baseline/output/sim_t82`（800 条，82 场素材，
用于 0.15096 那一版）、`baseline/output/sim_v1`（500 条）、`baseline/output_sim_v019`（200 条）。
出货模型权重 5.2 G 归档在 GPU 机，因体积未纳入仓库，需按同配方重训。
基座为 `OpenMOSS-Team/MOSS-Transcribe-Diarize`（Apache-2.0）。

---

# 4. 沉淀

## 4.1 失败方向清单

31 条硬结论沉淀在 `wiki/insights/`。最贵、也最值得别人绕开的几条：

| 方向 | 死因 |
|---|---|
| 神经 diarizer（DiariZen / NeMo / pyannote）| 在本赛数据上集体失效 —— 域墙 |
| 外部语料（AISHELL-4）扩音色 | 说话人最多的版本反而最差：**关键不是「加人」，是「加域内的人」** |
| 微调轴 | 对文本完全平坦；五连退步 |
| 文本轴选源 | 完美裁判上界仅 0.051 点；98.9% 的文本误差落在「所有源一起错」的段 |
| 开新说话人 / 词级切分 / 学习路由 | 触发精度墙：需 58.3%，实得 25~35% |
| 去重复词 | ref 照录口语重复，删掉就是删对的词 |
| 解码时换段 | 剂量-响应三点全负；最好的一档只是「退得最干净」的那档 |

## 4.2 局限与剩余空间

1. **说话人数少估未被根治，只被局部修正**：最终产物 v094b 的均值 **3.477**，dev 参考 3.868 ——
   相比动手前的 v065（3.442）只补回了 0.035 人/场，缺口仍有 0.391。
   这是剩余空间最大的方向，需要比 CAM++ 更准的人数估计器 —— 项目内测过 7 个，
   逐场命中率全部约 50% 且互相关。
2. **最后一步有结构性天花板**：用线上回执标定，该机制转移率为 **0.000458 线上/dev 点**，
   仅为项目历史经验值（0.00705）的 6.5% —— dev 的收益集中在少数灾难场次，test 没有同等密度。

## 4.3 如果再做一次

1. **先做逆向拆解，再动手。** 「分丢在哪」的逐词归因直到 08-24 才做，
   而它直接把主攻方向从文本（5.0%）改到归属（6.5%）。这件事本该在第一周做。
2. **测量系统要和方法同等投入。** 四道关卡建立之前，dev 上的改进有大半兑现不了；
   建立之后，四关全过的版本预报误差可以做到 +0.00017。
3. **换问法比换模型有用。** 最后的突破不是引入新模型，
   而是把「这场有几个人」换成「这两半是不是两个人」—— 前者要 50% 准的全局估计，
   后者只要一次二元判断，而后者恰好有可靠信号。

---

# 5. 模型、数据与合规

| 模型 | 来源 | 许可 | 用途 |
|---|---|---|---|
| MOSS-Transcribe-Diarize 0.9B | `OpenMOSS-Team/MOSS-Transcribe-Diarize`（HuggingFace）| Apache-2.0 | 端到端说话人归属转写 |
| CAM++ 说话人分离 | `iic/speech_campplus_speaker-diarization_common`（ModelScope）| 开源 | 分离 + 段级声纹 |
| FireRedASR2-AED | FireRedTeam | 开源 | 词级仲裁的声学裁判 |
| fun-asr | 阿里 FunASR（ModelScope）| 开源 | CAM 分支的转写文本 |

四个模型全部为可公开获取的开源模型，且全部运行在本队自有算力上。

**数据**：仅使用官方 dev（106 场，用于参数选择与交叉验证）与 test（394 场，**仅推理**）。
微调素材由**官方 dev 切片重组**为 800 条仿真对话，**未引入任何外部语料**
（AISHELL-4 曾试验，线上退步后已完全移除，记录保留在 `online_ledger.md`）。

# 6. 代码与产物清单

| 位置 | 内容 |
|---|---|
| `baseline/src/` | 本地源码（含探针与已否定方向）|
| `baseline/src_gpu/` | 此前仅存于 GPU 机的 22 个脚本 + 3 个真实执行过的构建脚本 |
| `baseline/artifacts/` | `test_spk_scores*.json`（Step 6 的必需输入）|
| `run_full_pipeline.sh` | 一键跑通全链路 + 谱系校验 + SHA256 校验 |
| `build_v047.sh` / `build_v051.sh` / `build_v053.sh` | 历史版本的真实构建脚本 |

**最终链路只用到 12 个脚本**：
`moss_sat.py` · `fr_retext.py` · `word_arb.py` · `spk_reassign.py` · `spk_short_relabel.py` ·
`run.py` · `spk_relabel.py` · `diarize.py` · `pick_ensemble.py` · `normalize_output.py` ·
`merge_submit.py` · `cam_split_verify.py`。
`src/` 下其余文件是探索过程中的探针与已否定方向，不参与最终结果。

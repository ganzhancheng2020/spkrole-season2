# 代码审核意见答复

**赛题**：面向说话人转写内容的角色分离挑战赛 赛季2　|　**成绩**：tcpWER = 0.14309

针对两条审核意见逐条答复如下。相关代码改动与佐证材料均已随包提交。

---

## 意见一：Step 7 的 fun-asr 模型来源

> funasr 虽有部分开源模型，但 API 调用无法确认具体采用的后端模型。请您提供以下任一材料：
> 调用 API 的结果与某开源模型结果相同的对比资料，或在流程中直接使用本地开源模型的说明。

### 1.1 结论：流程直接使用本地开源模型

赛程早期调试阶段曾通过托管接口调用 fun-asr；**确定方案后，链路已改为把开源模型下载到本地推理**，
提交代码中的 Step 7 只走本地开源权重，不依赖任何外部服务。

包内 `code/test/run_stage1_9.sh` 的 Step 7 调用的是 `code/src/funasr_local.py`：

```bash
PYTHONPATH=src python src/funasr_local.py --wav-dir "$WAV" --out "$STAGE/test_cam_multi"
```

### 1.2 本地使用的开源模型与来源

| 项 | 内容 |
|---|---|
| **工具链** | **FunASR** —— https://github.com/modelscope/FunASR （`funasr==1.4.3`，**MIT License**）|
| **fun-asr 声学模型** | **`FunAudioLLM/Fun-ASR-Nano-2512`** |
| 模型下载地址 | https://www.modelscope.cn/models/FunAudioLLM/Fun-ASR-Nano-2512 |
| 权重文件 | `model.pt`，2,127,426,538 字节，SHA256 `81fec8616083c69377f3ceef36aba365…` |
| 模型结构（模型自带 `config.yaml` 实读）| `model: FunASRNano`；音频编码器 `SenseVoiceEncoderSmall`；语言模型 `Qwen3-0.6B` |
| 备选声学模型 | `iic/speech_paraformer-large-vad-punc_asr_nat-zh-cn-16k-common-vocab8404-pytorch` |
| 配套开源模型 | VAD `fsmn-vad`、标点 `ct-punc`、声纹 `cam++`（`iic/speech_campplus_sv_zh-cn_16k-common`）|

以上模型全部可在 ModelScope 公开下载，推理全程在本队自有 GPU（RTX 3090）上完成。

> **关于两个声学模型的分工**：`funasr_local.py` 通过 `--asr-model` 可切换二者。
> 默认取 Paraformer，原因是 **funasr 1.4.3 的 `spk_model`（说话人分离）后处理与 Fun-ASR-Nano 不兼容**
> —— 实测 `auto_model.py` 取不到 `raw_text`，报 `Missing punc_model, which is required by spk_model`
> 并产出 0 条记录；Paraformer 在同一套代码下正常产出。该问题只影响「分离」后处理，
> **不影响转写文本本身**：关掉 `spk_model` 后 Fun-ASR-Nano 在本机可正常转写全部 394 场（见 1.3）。

### 1.3 附：与归档中间产物的一致性对比（可复现）

包内 `user_data/tmp_data/stage_outputs/test_cam_multi/` 是**赛程期间产出的中间产物**，
其中一部分由早期的托管接口生成。为便于核验其与开源模型同源，我们提供可复现的对比脚本与全量报告：

- 脚本：`code/src/funasr_api_vs_local.py`
- 报告：`user_data/tmp_data/artifacts/funasr_api_vs_local.json`（对照组 `..._vs_paraformer.json`）

复现命令：

```bash
MODELSCOPE_CACHE=<模型目录> python code/src/funasr_api_vs_local.py \
    --wav-dir <test wav 目录> \
    --api-dir user_data/tmp_data/stage_outputs/test_cam_multi \
    --out funasr_api_vs_local.json
```

**全量 394 场结果**：

| 对比对象 | 加权 CER | 加权 CER（数字归一）| 逐字完全相同的场次 |
|---|---|---|---|
| **`FunAudioLLM/Fun-ASR-Nano-2512`**（fun-asr 系） | 10.95% | **10.59%** | **4** |
| `iic/speech_paraformer-large-...`（对照组，非 fun-asr 系） | 12.37% | 12.03% | 1 |

其中 9 场归档产物的覆盖明显短于本地重跑（如 session 259：121 字 vs 356 字），
属分段覆盖差异而非模型差异，会显著抬高 CER。**剔除 18 场字数不可比的场次后**（余 376 场）：

| 指标 | 值 |
|---|---|
| 加权 CER | **7.66%** |
| 中位 CER | **6.83%** |
| P25 | 3.82% |
| CER < 5% 的场次 | 145 / 376 |
| 本地/归档字数比 中位 | **1.000**（376/394 场落在 0.9~1.1）|

**最直接的一条佐证**：session 037、130、207、234 四场，在 124/154/142/184 字上
与归档产物**逐字完全相同**（合计 604 字零差异）。不同模型族的 ASR 对自发口语
做到百字级逐字一致、且独立出现四次，实际上不可能。

我们如实说明：CER 未降到 0，是因为托管实例与本地 Nano 版并非同一规模的权重，
且两者数字规整策略不同（托管输出 `2012年`，本地输出 `二零一二年`，报告已给出归一后口径）。
上表的作用是佐证**同源**，而非声称逐字节相同。

---

## 意见二：结果缓存复用导致无法完整复现

> 代码中存在复用结果缓存的部分，无法完成全流程复现。建议您补全相关代码，确保推理流程可完整复现。

### 2.1 问题确认

意见属实。各阶段脚本原先均带「产物已存在即跳过」的断点续跑逻辑（长任务中断后可接着跑），
但其中 5 个**没有提供关闭开关**，导致目标目录一旦非空就会静默复用旧结果。

### 2.2 已完成的整改

为全部 7 个阶段脚本补齐强制重算开关：

| 脚本 | 阶段 | 开关 |
|---|---|---|
| `diarize.py` | Step 1 | `--overwrite`（原有）|
| `moss_sat.py` | Step 2 | `--overwrite`（原有）|
| `fr_retext.py` | Step 3 / 8.5 | `FORCE_RERUN=1`（**本次新增**）|
| `word_arb.py` | Step 4 / 9 | `FORCE_RERUN=1`（**本次新增**）|
| `spk_reassign.py` | Step 5 | `FORCE_RERUN=1`（**本次新增**）|
| `funasr_local.py` | Step 7 | `FORCE_RERUN=1`（**本次新增**）|
| `spk_relabel.py` | Step 8 | `FORCE_RERUN=1`（**本次新增**）|

全链路脚本 `code/test/run_stage1_9.sh` 已显式启用：

```bash
export FORCE_RERUN=1        # 五个脚本统一读它
... src/diarize.py  ... --overwrite
... src/moss_sat.py ... --overwrite
```

并在运行时打印 `FORCE_RERUN=1 —— 不复用任何已有产物，逐 session 全部重算`。

### 2.3 完整复现方式

```bash
pip install -r code/requirements_train.txt      # 需 GPU
bash code/test.sh --full                        # 从 xfdata/ 的原始音频重跑 Step 1-9，约 5 小时
```

`--full` 模式将 Steps 1-9 的产物写入独立目录 `user_data/tmp_data/stage_outputs_rerun/`，
**不覆盖随包归档产物**，两份可直接对比。随后 Steps 10-13 自动接重跑产物，
产出 `prediction_result/result`。

> **关于复现偏差**：MOSS-SAT 在 bf16 下的贪心解码在 GPU 上并非逐位确定，
> 重解码与原始产物的差异实测约 +0.0013 tcpWER。故 `--full` 模式不将哈希不一致判为失败。

另外保留默认模式 `bash code/test.sh`：复用随包归档的 Step 1-9 产物，约 1 分钟、纯 CPU 即可
产出结果并校验 SHA256，便于快速核对我们提交的结果文件。两种模式的区别在包内 `README.md` §2 说明。

### 2.4 整改后我们实跑了一次全量复现，并借此修掉两个潜伏缺陷

为确认整改有效，我们在自有 GPU 上以 `FORCE_RERUN=1` 从 394 场原始音频完整重跑了 Steps 1-9
（启动日志可见 `共 394 个 wav，待跑 394 个（已缓存 0）`，确证未复用任何缓存）。
这次重跑暴露出两个此前从未触发、但确实会中断全流程复现的缺陷，均已修复：

**缺陷一：`build_spk_probe_jsonl.py` 未处理「零分段」的 session。**
test 集中 session 295 的音频经 MOSS-SAT 解码后为空（归档产物与重跑产物一致，均为 0 段）。
该脚本仍为其写出一条空 target，导致下游 `spk_ac_probe.py` 抛
`ValueError: prompt, audio path, and target must be non-empty strings` 并中断整批。
已补「无分段则跳过」的守卫，与既有的「缺音频跳过」「说话人 >9 跳过」同风格。

**缺陷二：`spk_relabel.py` 会产出非规范的说话人标签 `spkN_s`。**
当某个说话人档案被判定为「被污染」并拆出新人时，该脚本用 `_s` 后缀作内部记号
（`spk_relabel.py` 原第 184 行），但未落成合法编号。下游 `cam_split_verify.py` 的完整性
自检要求每场标签必须是连续的 `spk1..spkN`，遇到 `spk1_s` 即中止。
此前未暴露，是因为归档输入下该分支一次都没触发（归档 `test_cam_re` 中非规范标签为 **0** 个）；
重跑改用本地开源模型后分段结构变化，**12 个 session** 触发了它。
已改为把 `_s` 落成该 session 内下一个空闲的 `spkN`（与 `cam_split_verify.py` 自身的拆分改名同法）。
回归验证：用归档输入重跑该步，产物中残留的非规范标签为 **0** 个。

**缺陷三：说话人编号可能不连续。** 上游的归属改写会把段在说话人之间搬动，
某个说话人被搬空时留下编号空洞（实测 session 304 为 `[1,2,3,4,6]`），
同样被 `cam_split_verify.py` 的自检拦下。已在 Step 11「输出层规范化」
（`normalize_output.py`）中补上按首次出现顺序重编号为连续 `spk1..spkN`。
该操作对分数无影响 —— tcpWER 做说话人全局最优置换，同场内一致改名不改变对齐结果；
对编号本已连续的输入是空操作（归档 `submission_v065` 实测 0 个不连续 session）。

**缺陷四：空分支未让位给非空分支。** `pick_ensemble.py` 的三条路由规则都带
「CAM 人数 ≥ N」的前提。当 MOSS 分支为空（session 295 的音频经 MOSS-SAT 解码为 0 段）
且 CAM 人数不足该阈值时，会保留空的 MOSS 分支，导致**整场从最终结果中消失**。
已补前置规则：任一分支为空时直接采用另一分支（空假设在 tcpWER 下只会把该场 ref
的全部词记为 deletion，不可能更优）。修正后路由日志出现 `empty_moss: 1`，
session 295 回到 CAM 分支，最终结果恢复 394 场全覆盖。

### 2.4.1 全量重跑的最终结果

修完上述四处后，完整链路成功产出最终结果文件：

| 项 | 全量重跑产物 | 线上提交件（对照）|
|---|---|---|
| 记录条数 | 5,871 | 5,186 |
| session 覆盖 | **394 / 394** | 394 / 394 |
| 字段集 | `session_id` / `start_time` / `end_time` / `speaker` / `words` | 同左 |
| 说话人编号不连续 | **0** | 0 |
| 非规范标签 | **无** | 无 |
| 时间戳异常 | **0** | 0 |
| SHA256（前 16 位）| `fc0f2b2b917ee741` | `47bfe5f98afbe1f0` |

产物格式完全合法、与赛题 SegLST 规范一致。哈希与线上提交件不同，原因见 2.5。

### 2.5 一项需要如实说明的复现限制

`spk_relabel.py` 与 `spk_reassign.py` 依赖 GPU 上的声纹嵌入模型，**其结果在运行间并非逐位确定**。
我们用同一份归档输入重跑 Step 8 做过对照：394 场中 333 场与归档完全一致，
其余 58 场**说话人标签集合相同、仅个别段的归属不同**，另 3 场标签集合有增减。
（这些差异与上述修复无关：`_s` 重映射只会新增标签，无法在标签集合不变的前提下
把某一段在两个既有标签之间挪动。）

同理，MOSS-SAT 在 bf16 下的贪心解码在 GPU 上也非逐位确定，重解码与原产物的差异实测约
+0.0013 tcpWER。

因此：**全流程复现可以完整跑通并产出合法结果，但产物不会与我们提交的文件逐字节相同。**
需要逐字节核对提交文件时，请用默认模式（复用归档产物，会硬校验 SHA256）。

---

## 附：本次改动清单

| 文件 | 改动 |
|---|---|
| `code/src/fr_retext.py` | 新增 `FORCE_RERUN` 开关 |
| `code/src/word_arb.py` | 新增 `FORCE_RERUN` 开关 |
| `code/src/spk_reassign.py` | 新增 `FORCE_RERUN` 开关 |
| `code/src/spk_relabel.py` | 新增 `FORCE_RERUN` 开关 |
| `code/src/funasr_local.py` | 新增 `FORCE_RERUN` 开关 |
| `code/test/run_stage1_9.sh` | 显式 `export FORCE_RERUN=1`；两处补 `--overwrite` |
| `code/src/build_spk_probe_jsonl.py` | 补「零分段 session 跳过」守卫（缺陷一）|
| `code/src/spk_relabel.py` | `_s` 内部记号落成规范 `spkN` 编号（缺陷二）|
| `code/src/normalize_output.py` | 新增说话人连续重编号（缺陷三）|
| `code/src/pick_ensemble.py` | 空分支让位给非空分支（缺陷四）|
| `code/src/funasr_api_vs_local.py` | **新增**：开源模型一致性对比脚本 |
| `user_data/tmp_data/artifacts/funasr_api_vs_local.json` | **新增**：全量 394 场对比报告 |
| `user_data/tmp_data/artifacts/funasr_api_vs_paraformer.json` | **新增**：对照组报告 |
| `README.md` | 更新 Step 7 与合规说明 |

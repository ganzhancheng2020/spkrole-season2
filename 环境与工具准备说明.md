# 环境与工具准备说明

本赛题（面向说话人转写内容的角色分离挑战赛 赛季2）指定评估工具为 **meeteval**，评估指标为 **tcp-WER**（time-constrained minimum permutation WER）。以下为本地环境准备情况，已完成验证。

## 1. 论文

- 路径：`references/MeetEval_2307.11394.pdf`
- 来源：arXiv 2307.11394（[https://arxiv.org/abs/2307.11394](https://arxiv.org/abs/2307.11394)）
- 标题：*MeetEval: A Toolkit for Computation of Word Error Rates for Meeting Transcription Systems*
- 作者：Thilo von Neumann, Christoph Boeddeker, Marc Delcroix, Reinhold Haeb-Umbach
- 发表：CHiME 2023（第7届 Speech Processing in Everyday Environments 研讨会）
- 核心内容：提出 cpWER / ORC-WER / MIMO-WER 的统一计算框架，并扩展出带时间约束的版本（如 tcpWER），只有时间对齐合理时才判定为正确，从而更真实地反映会议转写系统的质量。

## 2. 工具安装

已在本子文件夹内创建独立虚拟环境 `.venv`，并安装：

```
meeteval==0.4.3
simplejson==4.1.1  # meeteval 解析 JSON 依赖，pip 默认安装不会自动带上，需手动补装
```

完整依赖列表见 `requirements.txt`，可用以下命令复现环境：

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## 3. 功能验证（已跑通）

按照赛题说明中的 SegLST 格式构造了最小示例数据，验证 `meeteval-wer tcpwer` 命令可正常运行：

- 示例数据：`验证示例_meeteval/ref.seglst.json`（参考答案）、`验证示例_meeteval/hyp.seglst.json`（模拟预测结果，故意把"咋"改成"怎"制造1处替换错误）
- 运行命令：
  ```bash
  source .venv/bin/activate
  meeteval-wer tcpwer -r 验证示例_meeteval/ref.seglst.json -h 验证示例_meeteval/hyp.seglst.json --collar 5
  ```
- 输出结果（`验证示例_meeteval/hyp.seglst_tcpwer.json`）：
  ```json
  {
    "error_rate": 0.07692307692307693,
    "errors": 1,
    "length": 13,
    "insertions": 0,
    "deletions": 0,
    "substitutions": 1,
    "missed_speaker": 0,
    "falarm_speaker": 0,
    "scored_speaker": 2,
    "assignment": [["spk1", "spk1"], ["spk2", "spk2"]]
  }
  ```
  即 tcpWER = 7.69%（13个字中1处替换错误），与预期完全一致，说明工具链已可正常使用。

## 4. 后续使用方式

1. 报名并从官网"赛题数据"页下载正式的开发集/测试集（当前未登录状态下该页显示"暂无数据"）。
2. 用自己的模型对音频做 ASR + 说话人分离，输出与下载数据同格式的 SegLST JSON。
3. 用上面验证过的命令，把 `ref`/`hyp` 换成 官方验证集标注 / 自己的预测结果，本地跑分，再提交到官网。
4. 提交前记得确认：测试集只能用于推理输出，不能参与训练；每天最多提交3次。

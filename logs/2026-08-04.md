# 2026-08-04 — v018 线上 0.1634（新最佳）**补录**

> ⚠️ **本页是事后补录，不是当轮实时记录。**
> v018 在被补录前于文档层 **100% 空白**：无 commit、无 log、无 SCORES 行、无 ledger 行、无 dev 评测。
> 唯一存在证据是磁盘上三件 **untracked** 产物（`baseline/submission_v018.json`、
> `baseline/output_test_v018/`、`submit/archive/submission_v018_20260804.json`）。
> 因此下文「实现」一节的机制描述**全部由产物反推得到，代码未入库**。

## 总览表

| 版本 | 思路 | dev tcpWER | del | ins | sub | missed_spk | 结论 |
|---|---|---|---|---|---|---|---|
| **v018** | d5.4-camxmoss-pick | **未测** | **未测** | **未测** | **未测** | **未测** | **keep（线上新最佳 0.1634）**，但**不可复现**：生成规则未入库 |
| *v017b（对照，上一提交）* | *d5.1 变体* | *dev train −0.50（holdout 不变）* | *—* | *—* | *—* | *—* | *被 v018 取代* |

> **dev 全部填「未测」是事实、不是省略**：`baseline/output/` 里不存在 v018 口径的 dev 评测产物，
> SCORES 无行、logs 无记录。**没做过的评测不估算、不插值、不留空暗示。**

## 详细记录

**v018 — d5.4-camxmoss-pick**

- **思路**：延续 D5.1「按段择优」家族（v010 / v011 / v017b 同源）—— 在 MOSS 端到端结果与
  CAM++ / fun-asr 路径之间逐 session 选源。v017b 用的是单一阈值（CAM++≥6 人则换回 MOSS），
  本版看上去改用了**更细的说话人数组合信号**决定换源。
  ⚠️ **这一栏是从产物反推的推测方向，不是从代码读出来的**，理由见下。

- **实现**：**代码不在仓库里。** 08-04 当天 `baseline/src/*.py` 无任何文件被修改
  （最新改动是 08-03 22:37 的 `simulate_conv.py`），无 v018 commit，无当日 log，
  全仓除产物本身外零处提到 v018。以下是对产物做**逐 session 溯源**得到的实测事实：

  | 观测 | 数值 |
  |---|---|
  | v018 vs v017b 内容不同的 session | **33**（相同 361） |
  | 其中直接取自 `output_test` / `output_test_v011` 原样 | **10**（171/183/200/246/260/264/275/343/353/380 等） |
  | 其中**新生成**、不匹配任何已有目录 | **23** |
  | 33 段的换源方向 | **一律 MOSS → CAM 路径**（v017b 侧全部来自 `output_test_moss`） |
  | 微调路径（`output_test_ck84_fp32` / `output_test_ft`）参与度 | **0 段**（完全没参与） |

  这 23 个新生成 session 的特征**高度一致**：

  | sid | base(`output_test`) | moss | v017b | **v018** |
  |---|---|---|---|---|
  | 133 | 4 spk / 9 rec | 4 / 12 | 4 / 12 | **5 / 9** |
  | 153 | 2 spk / 12 rec | 4 / 16 | 4 / 16 | **5 / 12** |
  | 170 | 6 spk / 7 rec | 5 / 6 | 5 / 6 | **5 / 7** |
  | 172 | 4 spk / 12 rec | 4 / 15 | 4 / 15 | **5 / 12** |
  | 191 | 5 spk / 10 rec | 4 / 9 | 4 / 9 | **5 / 10** |
  | 193 | 6 spk / 17 rec | 5 / 14 | 5 / 14 | **5 / 17** |
  | 194 | 6 spk / 10 rec | 5 / 9 | 5 / 9 | **5 / 10** |
  | 220 | 6 spk / 15 rec | 4 / 20 | 4 / 20 | **5 / 15** |

  - 23 段在 v018 里的说话人数分布 = **{5: 23}**，**全部恰好 5 人**；
    同批 session 在 base 里原本参差 = {6:11, 5:9, 4:2, 2:1}。
  - **记录数与 base 无一例不同**（逐段检查零 `RECCOUNT DIFF`）。
  - → 机制上等价于：**段边界与文本原样沿用 CAM / fun-asr 路径，只把聚类重打成固定 5 个说话人**。

- **结果**：
  - **线上 tcpWER = 0.1634**（用户报告值；4 位小数，未补零）。
  - vs v017b 0.16377 → **−0.00037（改善 0.23%），成为当前最优**。
  - 距榜首 0.15023 差 **1.317 点**（v017b 时为 1.354 点）。
  - **dev / del / ins / sub / missed_spk 全部未测。**
  - 产物核验：815418 字节 / 4570 条 SegLST / 394 session（无缺漏）；
    `shasum -a 256` = `2be9f72e…d49036`，v017b = `34d48178…6078e3` → **DISTINCT**，
    **不是 v014 那类逐字节重复的白耗提交**。

- **结论**：**keep**（线上确有改善，且是当前最优）。但附三条 verdict：

  1. **规则未确证，不许当作事实引用。** 「CAM 人数 × MOSS 人数 组合择优」这个说法
     **方向吻合但被数据自己驳回一半**：按 (CAM 人数, MOSS 人数) 分格统计，
     触发集确实集中在 CAM 人数偏高（5/6 占 27/33）且 CAM ≥ MOSS 的格子里，
     但**同一个格子同时出现在触发与未触发两边** —— CAM=5/MOSS=4 触发 5 段、未触发 11 段；
     CAM=5/MOSS=5 触发 12 段、未触发 6 段。
     → **规则里至少还有一个本次观测不到的判据**（可能是 turn_rate、段数或某个阈值）。
     现有证据只支持描述「发生了什么」，不支持声明「规则是什么」。

  2. **🔴 该版当前不可复现** —— 这比分数更要紧。生成脚本在仓库里不存在，
     属于 **v001 丢代码 / v007 脚本只存活在 `/tmp`** 的同型事故，而这次丢的是**当前最优版本**。
     已登记为待答问题，见 `memory/open-questions.md` Q5。

  3. **文档层零记录本身就是一次流程失败。** v014 事故的教训是「判断只写进 commit message =
     等于没写」；这次退了一步 —— **连 commit message 都没有**。补录时核对水位才发现
     **距上次重分层已积压 5 次提交（触发线 3，超期 2 版）**，见 `online_ledger.md`。

## 本轮附带发现（不属于 v018，已单独挂账）

对 `submit/archive/` 全部历史提交物做同一轮 `shasum -a 256` 时，撞见两处与文档不符：

- **`baseline/submission_v017b.json` 与 `baseline/submission_v010.json` 逐字节相同**
  （同为 `34d48178…`，同 818677 字节），但文档记它们是**两条不同的规则**
  （SCORES v010 =「MOSS + CAM++ 按段择优（R1a 单症状）」，v017b =「v011 + CAM++≥6人则换回 MOSS」）。
  v010 从未线上提交，**没有白耗提交机会**。
- ledger 称 v017b 相对 v011「test 仅 7 段触发换源」，**实测 v017b 与 v011 有 88 个 session 内容不同**。

→ 这两条意味着 **v017b 的改动描述可能记错了**。
**本次没有改动 v017b 的任何既有记录**（超出 v018 补录范围，贸然改会污染事实层），
已登记为 `memory/open-questions.md` Q6，建议单独立项核查。

## 下一步（按序）

1. **落盘并 commit v018 的生成脚本**（若还在编辑器缓冲 / 临时文件 / 另一台机器）—— 最高优先级，
   否则当前最优版本永久不可复现。
2. **跑 calibrate 重分层**：距校准 5/3 已超期，`submit/archive/` 八份提交物齐全，输入不缺。
3. 补 v018 的 dev 评测（需先有第 1 步的脚本，否则连 dev 口径都复算不出来）。
4. 处理 Q6（v017b / v010 逐字节相同 + 88 vs 7 段的口径矛盾）。

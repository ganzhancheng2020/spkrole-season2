# 2026-08-27

## 🔴🔴 战略性更正：排名按历史最低成绩，掉分不伤排名

`SCORES.md:102` 与 `HANDOFF.md:24` 两处独立记录：**赛题 §104 —— 排行榜按团队历史最低成绩排名。**

**我此前"四关不过就不出货"的纪律优化错了目标。**
四关正确预测了**分数**（4/4：v048/v051 过关→涨，v052/v053 挂关→跌），
但真正的目标是**排名**，而排名只看历史最好成绩：

| 提交结果 | 对分数 | 对排名 |
|---|---|---|
| 改善 | 刷新最好 | **上升** |
| 退步 | 无影响（最好成绩不变）| **零影响** |

→ **每次提交都是零成本彩票。** v052/v053 的"累计代价 0.00230"**根本不存在**。
因为这个错误，8/26 一整天 0 次额度使用，白白浪费。

**修正后的策略：在额度允许范围内尽可能多地投出独立的候选**，
优先级按 dev 期望增益排序，**不再因四关不过而弃投**（四关仍用于排序，不再用于否决）。

## v054：多信号融合路由器（首个按新策略出货的版本）

- **模型**：LogReg（11 个声学特征），用全部 106 段 dev 训练（test 独立，无泄漏）
- **阈值**：0.70（嵌套验证各折独立选出 0.63~0.77）
- **dev 估计**：嵌套完全样本外 **−0.242 点**（单层 CV 的 −0.859 已知被选阈值虚增 72%）
- **四关**：挂第 2、3 关 —— **按新策略不再作为否决理由**

test 侧信号全部重算（393 嵌入 ×2、394 变化点、393 余量）。

**产物** `submit/submission_v054.json`（5245 条 / 394 段 / `8f9a250f`）：
改变 **82 段**路由，分布 262/132 → **340/54**（大幅减少 CAM 使用），无退化 session。

**线上回执 v054 = 0.14709**（vs v051 0.14507，+0.00202）。
四关（挂 2、3 关）**第 5 次正确预测方向** —— 但按 §104 排名规则，
最好成绩仍是 v051 的 0.14507，**排名未受影响**。策略修正本身是对的：
代价确实是零，只是这张彩票没中。

## v055：定向拆分说话人（补文档 —— 本版此前无 build 脚本、无记录）

⚠️ 本版产物在 8/27 已生成并归档，但**无 build 脚本、无日志**，属 v018 型记录缺口。
现从产物反推补录（`_b` 后缀标签是唯一指纹）。

- **机制**：保留 MOSS 全部结构，只把**某一个说话人**用 2-means 拆成两个
  （标签加 `_b` 后缀），由 tcpWER 的全局最优映射决定要不要用这个细分。
  选哪个说话人拆 = 双峰性（塌缩检测）最高者，门限 τ=0.60。
- **针对**：hyp 系统性**少判**说话人（dev 上少判 30 段 / 多判 17 段）。
- **与已死机制的区别**：全量重聚类（丢掉 MOSS 标签重推）dev **+5.07 点**；
  本机制**保留 MOSS 标签只细分**，dev **−0.118 点**。
- **dev 上界探针**：Δ**−0.710**、11 段全改善 0 恶化、剔 top3 −0.314、P=100%，**四关全过**
  —— 上界合格，无 ref 准则只够到 −0.118。
- **四关**：挂第 1、2 关（触发 5 段 < 8）。按新策略**不作为否决理由**。

**产物** `submit/submission_v055.json`（4988 条 / 394 段 / `60766c82`）：
与 v051 **切分和文本逐条相同**，仅 **56 条说话人不同**；
**25 段**的说话人数 +1（369 段不变、0 段减少），无退化 session。
单变量干净，与 v051 只差「拆不拆」这一个决策。

## v056：条件化定向拆分（v055 的改进版）

**动机**：v055 的准则（无条件取双峰性最高者，τ=0.60）只够到 −0.118，
而其上界探针是 −0.710（四关全过）。准则本身有改进空间。

**改进**：加一个**不是拟合出来的**条件 —— 只在 `hyp 说话人数 ≤ 3` 的 session 拆。
依据是实测的**系统性少判**（dev：少判 30 段 / 多判 17 段），人数少的 session 是少判高发区。

| 配置 | Δ点 | 四关 |
|---|---|---|
| τ0.60 无条件（= v055）| −0.118 | 挂 1、2 关 |
| **τ0.45 + 人数≤3** | **−0.314** | **[1234] 全过** ⭐ |
| τ0.55 + 人数≤3 | −0.257 | 挂第 1 关（触发 5 段）|
| τ0.45 + 人数≤4 | −0.242 | 挂第 2 关 |

**嵌套验证**（τ 与人数上限都折内选）：−0.314 → **−0.129**，第 2、3 关挂。
但**各折选出的配置高度一致**：`(0.45,3)×3、(0.5,3)、(0.45,4)` ——
不像融合路由器那次配置乱跳（0.63~0.77）。

⚠️ **口径对比要点**：v056 的 **−0.129 是诚实的样本外数**；
v055 的 −0.118 是**在同一批数据上选 τ=0.60 得到的样本内数**。
**同口径下 v056 更优。**

**产物** `submit/submission_v056.json`（4988 条 / 394 段 / `039ea635`）：
与 v051 **切分与文本逐条相同**，仅 **111 条说话人不同**；触发 **44 段**（v055 是 25），无退化 session。

**旁证**：说话人数分布向 ref 靠拢 ——
v051 `{2:66, 3:163, 4:96}` → v056 `{2:57, 3:137, 4:131}`，
而 dev ref 按比例折算到 394 段约为 `{2:37, 3:108, 4:142}`。
**v051 的 3 人段明显过多，v056 修正了这个偏斜方向。**

## 另测：说话人合并（v055 的镜像）—— 弃

针对多判的段，把声学最近的两个说话人并掉：τ0.70 → **+0.180**、τ0.75 → **+0.242**，
且只触发 2~4 段。**净负，不做候选。**

## word_arb 参数轴：两个分支都确认 0.05 最优（参数轴挖尽）

正确口径下补测 `MARGIN=0.07 / MAX_BLOCKS=6`：

| 分支 | 基线(0.05/6) | 0.07/6 | Δ点 | 四关 |
|---|---|---|---|---|
| CAM | 20.754% | 20.759% | **+0.005** | 挂 3 关 |
| MOSS | 16.810% | 16.800% | **−0.010** | 挂 3 关 |

**两个都是噪声级**（改善/恶化各约一半，剔 top3 均翻正）。
→ **v048 定下的 0.05/6 在两个分支上都是最优，参数轴真正挖尽。**

⚠️ 工程记录：8 配置并行**总吞吐反而下降**（2.8 段/分 vs 顺序 5 段/分）——
每个 word_arb 进程各载一份 4.4GB FireRed，机器内存带宽饱和。
加 `OMP_NUM_THREADS` 限制也没救。**结论：word_arb 只能顺序或至多 2 路并行。**
另试改 `use_gpu=True`：模型报告 use_gpu=True 但 GPU 利用率 0%、无产物，未生效，放弃。

## 拆分家族的三个工作点（零下行风险下的多张彩票）

| 版本 | 配置 | dev Δ点 | 四关 | test 触发 | 说话人不同 |
|---|---|---|---|---|---|
| v055 | τ0.60 无条件 | −0.118（样本内）| 挂 1、2 | 25 段 | 56 条 |
| **v056** | **τ0.45 + 人数≤3** | **−0.314 / 嵌套 −0.129** | **全过** | 44 段 | 111 条 |
| v057 | τ0.45 + 人数≤4 | −0.242 | 挂第 2 | 71 段 | 177 条 |
| v058 | τ0.55 + 人数≤3 | −0.257 | 挂第 1（触发 5）| 20 段 | 50 条 |

四版**切分与文本全部逐条相同**，只动说话人标签，均无退化 session。
v057 最广（71 段）、v058 最保守（改善 5 / 恶化 0）、v056 是 dev 最优且四关全过。

## 🎯 新轴：pick 的 turn_rate 阈值（v059）—— 且与拆分可叠加（v060）

出货链路里 `--moss-max-turn-rate 0.08` 是个很久没动的标量。此前只扫过**整个规则族**
（357 配置），从未单独扫过这一个。正确口径下扫：

| 阈值 | 用 CAM | Δ点 | 四关 |
|---|---|---|---|
| 0.0 ~ 0.04 | 17 段 | **−0.288** | [1x34] |
| 0.05 | 23 段 | −0.293 | [1x34] |
| **0.08（现行）** | 35 段 | — | 基线 |
| 0.12 | 24 段 | +0.211 | 挂 4 关 |

**0.0~0.04 是一个平台**（五个值结果完全相同）——等价于「**关掉这条规则**」，
不是拟合出来的尖峰，实质只是 1 bit 决策。
机制上也讲得通：这条规则把 session 路由给 **CAM（弱支 20.754% vs MOSS 16.810%）**，少用它就该更好。

### 叠加：与拆分家族 88% 可加

| | Δ点 | 四关 |
|---|---|---|
| ① 只改阈值 0.04 | −0.288 | [1x34] |
| ② 只拆分 τ0.45/人数≤3 | −0.314 | [1234] |
| **③ 叠加** | **−0.530** | **[1234] 全过**（剔 top3 −0.165、P=100%）|

−0.288 + −0.314 = −0.602，实得 −0.530 → **88% 可加**，两轴基本正交。

**嵌套验证**（路由阈值固定 0.04，拆分参数折内选）：−0.530 → **−0.345**，
第 2 关差 +0.015（基本贴零），1/3/4 关过；各折选出 `(0.45,3)×3、(0.5,3)、(0.45,4)` 一致。

**折线上预测 0.14507 − 0.345×0.00685 = 0.14271**（比 v051 好 0.00236）。

### 本轮候选总表（按 dev 诚实估计排序）

| 版本 | 机制 | dev（嵌套/诚实）| 四关 | 指纹 |
|---|---|---|---|---|
| **v060** | 路由0.04 + 拆分 | **−0.345** | 嵌套后挂第2关(+0.015) | `20ae81fc` |
| v059 | 仅路由0.04 | −0.288（平台）| [1x34] | `cc0e94ef` |
| v056 | 仅拆分 τ0.45/n≤3 | −0.129 | [1234] | `039ea635` |
| v057 | 拆分 τ0.45/n≤4 | −0.242（样本内）| [1x34] | `a50c1701` |
| v058 | 拆分 τ0.55/n≤3 | −0.257（样本内）| [x234] | `2db37a76` |
| v055 | 拆分 τ0.60 无条件 | −0.118（样本内）| 挂1、2 | `60766c82` |

### 另证否

- **交错度过滤**（拆出的两组是否时间交错）：方向**反**（有效组 0.439 < 有害组 0.515，
  区分度 −0.076），加这道门 −0.314 → −0.237。假设错误，弃。

### 补：pick 其余四个参数全部已在最优

以 `turn_rate=0.04` 为新基线，逐个扫其余四个硬编码参数：

| 参数 | 现行 | 偏离后最好 | 结论 |
|---|---|---|---|
| `moss_max_spk` | 2 | =1 → +0.370 | 现行最优 |
| `cam_min_spk` | 3 | =2 → +0.406 | 现行最优 |
| `cam_over_spk` | 6 | =5 → +0.391（7/9 无变化）| 现行最优 |
| `cam_eq5_moss_min` | 4 | =3 → +0.067 | 现行最优 |

**每个偏离都更差（+0.067 ~ +1.250）** →
**`turn_rate` 是整个规则族里唯一调错的参数。** 链路参数至此全部确认最优。

### 路由轴的简单规则空间也挖尽

turn_rate 拿到 −0.288 后，试了几个**从没当过路由特征**的量（只用单特征阈值，
因为学习模型 v046/v054 全死、简单规则却活）：

| 规则 | Δ点 |
|---|---|
| MOSS 每段词数 > 4.0 → MOSS | +0.869 |
| MOSS 段数 > 12 → MOSS | +0.638 |
| MOSS 段数 > 16 → MOSS | +0.165 |
| MOSS 段数 > 20 → MOSS | −0.010（仅触发 2 段）|

**全部为负**，没有第二条 turn_rate。路由轴的简单规则空间挖尽。

## 本轮最终候选表

| 序 | 版本 | 机制 | dev（诚实）| 指纹 |
|---|---|---|---|---|
| **1** | **v060** | 路由0.04 + 拆分τ0.45/n≤3 | **−0.345**（嵌套）| `20ae81fc` |
| 2 | v059 | 仅路由0.04 | −0.288（平台）| `cc0e94ef` |
| 3 | v061 | 路由0.04 + 拆分τ0.55/n≤3（保守）| 介于 −0.288 与 −0.345 | `ab70ab3e` |
| 4 | v056 | 仅拆分τ0.45/n≤3 | −0.129（嵌套）| `039ea635` |
| 备 | v057/v058/v055 | 拆分其他工作点 | 样本内 −0.24/−0.26/−0.12 | — |

v060 与 v059 是**同一叠加的拆解**：投这两个能直接读出「路由轴」与「拆分轴」
各自转移了多少，为后续方向提供依据。

## v062：三层叠加（短段改写调参 + 路由 + 拆分）

`spk_short_relabel` 的 `τ=1.5 / max_words=5` 来自 namespace-artifact 分析，
**从没在正确口径下扫过**。而 v060 把 MOSS 分支的服务面提到 **88%**（346/394），
这个旋钮的杠杆随之变大。扫（在 turn0.04 基线上）：

| τ / max_words | Δ点 | 四关 |
|---|---|---|
| 1.0 / 5 | −0.015 | 挂 2、3 |
| **1.5 / 5（现行）** | −0.010 | 挂 2、3 |
| 2.0 / 5 | +0.000 | — |
| **1.5 / 3** | **−0.077** | [1x34] |
| 1.5 / 8 | +0.000 | — |
| 1.5 / 12 | +0.165 | 挂 3 关 |

**`max_words` 从 5 收紧到 3 更好**（−0.077 vs −0.010）—— 与「标错的段是短段、
且越短越准」的既有结论一致。

### 三层叠加结果

| 组合 | Δ点 | 四关 |
|---|---|---|
| 两层：短段(1.5/3) + 路由 | −0.365 | 全过 |
| v060：路由 + 拆分 | −0.530 | 全过 |
| **v062：短段(1.5/3) + 路由 + 拆分** | **−0.596** | **全过**（剔 top3 **−0.231**、P=100%、改善 15/恶化 12）|

**产物** `submit/submission_v062.json`（5303 条 / 394 段 / `da881beb`）：
相对 v060 仅 **15 条说话人不同**（短段改写口径变化），切分与文本完全相同，无退化 session。

### 最终候选表

| 序 | 版本 | 机制 | dev 样本内 | 指纹 |
|---|---|---|---|---|
| **1** | **v062** | 短段1.5/3 + 路由0.04 + 拆分 | **−0.596** | `da881beb` |
| 2 | v060 | 路由0.04 + 拆分 | −0.530（嵌套 −0.345）| `20ae81fc` |
| 3 | v059 | 仅路由0.04 | −0.288（平台）| `cc0e94ef` |
| 4 | v056 | 仅拆分 | −0.314（嵌套 −0.129）| `039ea635` |

### v062 嵌套验证：**首个嵌套后仍四关全过的候选**

路由阈值固定 0.04（平台结构，1 bit 决策）；短段 `max_words` 与拆分 `(τ, 人数上限)` 折内选：

    各折选中: [(3,0.5,3), (3,0.45,4), (3,0.45,3), (3,0.45,3), (3,0.45,3)]
    Δ -0.411 点   触发 26   改善 14 / 恶化 12
    [1]OK  [2]剔top3 -0.051 OK  [3]P=95.6% OK  [4]OK   >>> 四关全过

**五折全部选中 `max_words=3`**，拆分 `(0.45,3)` 中 3/5 折 —— 配置高度稳定，
不像融合路由器那次阈值乱跳（0.63~0.77）。

样本内 −0.596 → 嵌套 **−0.411**（收缩 31%，远小于路由器那次的 72%）。

### 最终候选表（全部按诚实/嵌套口径）

| 序 | 版本 | 机制 | dev 诚实 | 预测线上 | vs v051 |
|---|---|---|---|---|---|
| **1** | **v062** | 短段1.5/3 + 路由0.04 + 拆分 | **−0.411**（嵌套，四关全过）| **0.14225** | **−0.00282** |
| 2 | v060 | 路由0.04 + 拆分 | −0.345（嵌套）| 0.14271 | −0.00236 |
| 3 | v059 | 仅路由0.04 | −0.288（平台）| 0.14310 | −0.00197 |
| 4 | v056 | 仅拆分 | −0.129（嵌套）| 0.14419 | −0.00088 |

距榜首 0.141 仍差 **0.00125**。

### 今日方法论收获：「继承来的默认值」是一条真脉

今天连续找到**三个从没被单独扫过的标量**，全属同一类：

| 旋钮 | 出处 | 收益 |
|---|---|---|
| `--moss-max-turn-rate 0.08 → 0.04` | pick 规则族，只在整族里扫过 | −0.288 |
| 拆分加「人数≤3」条件 | 来自实测的系统性少判 | −0.314 |
| `max_words 5 → 3` | namespace-artifact 分析定的，没扫过 | −0.077 |

**它们高度可加**（三层 −0.596 ≈ 三者和的 88%），因为作用在链路不同环节。

⚠️ **自我更正**：我 8/26 判「参数轴挖尽」是**错的**。我扫过 word_arb 和 pick 的**规则族**，
但漏了**散落在各环节的单个标量**。教训：**「族扫过了」不等于「族里每个标量都扫过了」。**

### 收尾审计：剩余常数全部确认（无更多可挖）

**廉价预检先行**（避免再浪费 GPU）：

| 常数 | 影响面 | 结论 |
|---|---|---|
| `word_arb MIN_SEG_SEC=0.2` | **0 段**（MOSS 最短段 0.43s）| **完全惰性，不必扫**（省一次 21min 运行）|
| `spk_relabel WIN_SEC=1.5` | CAM 段 **33% 短于它** | 影响面真实，但 CAM 仅服务 12%，杠杆 ≈0.06 点 |
| `spk_reassign MIN_PROFILE_WIN=3` | 机制本身只触发 2~5 段 | 惰性 |

**拆分机制自己的常数**（`min_items` 与 balance 归一化除数，是我引入时随手定的）：

| min_items / baldiv | Δ点 | 四关 |
|---|---|---|
| 3 / 0.30 | −0.550 | 挂第 4 |
| 4 / 0.20 | −0.370 | 挂第 4 |
| **4 / 0.30（现行）** | **−0.596** | **全过** ⭐ |
| 4 / 0.40 | −0.442 | 全过 |
| 5 / 0.30 | −0.519 | 全过 |
| 6 / 0.30 | −0.360 | 全过 |

**现行值即最优。** 至此出货链路所有可调标量全部确认最优或惰性。

## 今日总结

**v062 是终点候选**：dev 嵌套 **−0.411**、**四关全过**（首个通过该检验的版本）、
预测线上 **0.14225**（比 v051 好 0.00282，距榜首 0.141 仍差 0.00125）。

三个新旋钮全部来自同一类 —— **继承来的默认值从没被单独扫过**：

| 旋钮 | 收益 | 此前为何被漏 |
|---|---|---|
| `turn_rate 0.08→0.04` | −0.288 | 只在 357 配置的**规则族**里扫过，没单独扫 |
| 拆分加「人数≤3」 | −0.314 | 条件来自实测的系统性少判，此前没想到加条件 |
| `max_words 5→3` | −0.077 | namespace-artifact 分析定的，从没验证 |

**教训（已更正 8/26 的错误判断）：「族扫过了」≠「族里每个标量都扫过了」。**

## 🎯 v063：层间交互 —— 路由阈值在完整三层下重扫

三层是**按固定顺序调的**：路由 0.04 是在**没有拆分**时定的。层间有交互，故在完整三层下重扫：

| 路由阈值 | 用 CAM | Δ点 |
|---|---|---|
| 0.0 / 0.02 / 0.04 | 16 | −0.596（平台）|
| **0.05** | 22 | **−0.653** |
| 0.06 | 23 | −0.535 |
| 0.08 | 34 | −0.381 |

0.05 是**尖峰**（两侧都低），按纪律必须过嵌套。

### 四参数全嵌套（路由、max_words、τ、人数上限全部折内选）

    各折选中: (0.05,3,0.5,3), (0.05,3,0.45,3) ×4
    Δ -0.571 点   触发 22   改善 13 / 恶化 9
    [1]OK  [2]剔top3 -0.211 OK  [3]P=99.9% OK  [4]OK   >>> 四关全过

**五折全部独立选中「路由=0.05、max_words=3」** —— 那个尖峰不是噪声。
v062 把路由锁在 0.04 是**次优的**（0.04 是在无拆分条件下测的）。

**产物** `submit/submission_v063.json`（5180 条 / 394 段 / `825ea45f`）：
路由给 CAM 66 段、拆分触发 45 段，无退化 session；
与 v062 在同位置记录上**说话人与文本完全相同**（差异全在路由带来的切分变化）。

### 候选表（最终）

| 序 | 版本 | dev 诚实 | 预测线上 | vs v051 | vs 榜首 0.141 |
|---|---|---|---|---|---|
| **1** | **v063** | **−0.571**（四参数全嵌套，四关全过）| **0.14116** | **−0.00391** | **+0.00016** |
| 2 | v062 | −0.411（三参数嵌套）| 0.14225 | −0.00282 | +0.00125 |
| 3 | v060 | −0.345 | 0.14271 | −0.00236 | +0.00171 |

**v063 的预测值距榜首 0.141 只差 0.00016 —— 在迁移率的不确定度以内。**

### 方法论：调参顺序本身是一个变量

v062→v063 的全部增益（−0.411 → −0.571）来自**同一组机制的重新排序标定**，
没有引入任何新机制。**层间交互使「先调 A 再调 B」与「A、B 联合调」结果不同**，
而联合嵌套是唯一能同时避免次优与过拟合的做法。

### 收敛确认：更宽的联合搜索没有更好

把短段改写的 `τ`（此前一直锁 1.5）也放进联合网格，扩到 5 参数 24 配置：

    样本内最优 (srl_τ=1.0, mw=3, 路由=0.05, split_τ=0.45, n≤3) -> Δ-0.653
    各折选中: (1.0,3,0.05,0.5,3), (1.0,3,0.05,0.45,4), (1.0,3,0.05,0.45,3)×3
    五参数嵌套 Δ-0.468（四关全过）

**样本内 −0.653 与 v063 的配置同分** —— `srl_τ` 在 1.0 与 1.5 之间对该工作点无差别。
而**嵌套估计反而更低**（−0.468 < v063 的 −0.571）：网格变大后折间选择开始分歧
（有一折选了人数≤4），选择方差上升。

→ **v063 已在联合最优点，搜索收敛。** 再扩网格只增加选择方差，不增加真实收益。

⚠️ 这也是一条可推广的纪律：**嵌套估计随网格变大而变差时，说明已过最优搜索宽度**，
应停止扩张而非继续加参数。

## 今日最终交付

| 序 | 版本 | dev 诚实 | 预测线上 | vs v051 | vs 榜首 0.141 |
|---|---|---|---|---|---|
| **1** | **v063** `825ea45f` | **−0.571**（四参数嵌套，四关全过）| **0.14116** | **−0.00391** | **+0.00016** |
| 2 | v062 `da881beb` | −0.411 | 0.14225 | −0.00282 | +0.00125 |
| 3 | v059 `cc0e94ef` | −0.288 | 0.14310 | −0.00197 | +0.00210 |

三者是同一叠加的不同切面（v059=仅路由层、v062=三层但路由次优、v063=联合最优），
回执可直接标定每层的真实迁移率。

### 最后一个未扫常数：`spk_relabel WIN_SEC` —— 惰性，收工

v063 把路由给 CAM 的段提到 66/394（17% 杠杆，高于此前的 12%），
且 CAM 段有 **33% 短于 `WIN_SEC=1.5`** —— 嵌入窗比它要刻画的段还长，影响面看起来真实。

**廉价信号先行**（relabel 只需 5 分钟，word_arb 要 21 分钟）：

| WIN_SEC | CAM 分支标签质量 | 触发 |
|---|---|---|
| 1.5（现行）| 21.818% | — |
| 1.0 | 21.844%（**+0.026**）| **仅 1 段，改善 0/恶化 1** |

**窗长对 `spk_relabel` 基本惰性** —— 只改 1 段且改坏。当场停掉后续 word_arb，省约 40 分钟 GPU。

⚠️ 过程中踩了两个自己记录过的坑：
1. `WIN_SEC`/`WIN_SHIFT` 是**硬编码常量不读环境变量**，第一次扫参三个配置会跑出一模一样的结果
   —— 打补丁时加了断言（替换未命中即报错），杜绝静默失败。
2. `pgrep -f "spk_relabel[.]py"` 的模式串出现在**我自己的 ssh 命令行**里，把会话杀了
   —— 正是 `pgrep-f-self-match` 记录的坑。改用 `w[i]nsweep2` 式自排除后正常。

脚本已用备份还原（`spk_relabel.py.bak` → `spk_relabel.py`），链路未被污染。

**至此出货链路上所有可调标量全部扫完或确认惰性。v063 为最终交付。**

### 第三次收敛确认：按分支分设拆分阈值也没有更好

两支特征不同（CAM 段中位 2.44s vs MOSS 2.04s），此前一直用同一套 τ ——
这是个从没用过的自由度。4×4 网格：

| τ_MOSS / τ_CAM | 拆 M/C | Δ点 |
|---|---|---|
| 0.40 / 0.45 | 13/1 | −0.483 |
| 0.45 / 0.40 | 10/2 | −0.632 |
| **0.45 / 0.45（= v063）** | 10/1 | **−0.653** |
| 0.50 / 0.40 | 5/2 | −0.576 |

**对称阈值最优**；嵌套 −0.550，略低于 v063 的四参数嵌套 −0.571。

→ 三次独立的扩张尝试（加 srl_τ / 加分支特异 τ / 更宽路由网格）**全部没有超过 v063**。
**搜索已确认收敛。**

## 本日收官

**v063 = 最终交付**：dev 四参数嵌套 **−0.571**、四关全过、预测线上 **0.14116**。

出货链路上**所有**可调标量已扫完或确认惰性：

| 环节 | 标量 | 状态 |
|---|---|---|
| MOSS 解码 | greedy vs beam | beam +34.77（毁归属）|
| word_arb | MARGIN / MAX_BLOCKS | 0.05/6 最优（两分支各自确认）|
| word_arb | MIN_SEG_SEC | **影响 0 段，惰性** |
| pick | turn_rate | **0.08 → 0.05，−0.288** ⭐ |
| pick | 其余 4 参数 | 全部已在最优 |
| spk_short_relabel | τ / max_words | **max_words 5 → 3，−0.077** ⭐ |
| spk_reassign | COH_MIN / MARGIN / MIN_PROFILE_WIN | 机制仅触发 2~5 段，惰性 |
| spk_relabel | SPLIT_THR / MARGIN | 早前已扫，在平台 |
| spk_relabel | **WIN_SEC / WIN_SHIFT** | **仅改 1 段且改坏，惰性** |
| 拆分（新增）| τ / 人数上限 / min_items / baldiv / 分支特异 | **加「人数≤3」−0.314** ⭐；其余现行即最优 |
| 层间 | 调参顺序 | **联合标定 −0.160** ⭐ |

**下一步需要线上回执**：v059/v062/v063 三张票分别对应「仅路由层 / 三层但路由次优 / 联合最优」，
回执可标定每层真实迁移率；若某层缩水，按真实比例重新配比还能再出一版。

## 🔴 迁移率必须按机制族分开算（推翻了我自己的推荐顺序）

我此前所有预测都用 `TRANSFER=0.00685`，但那是从 **v048/v051（分支内改动）** 标定的。
按族拆开看历史回执：

| 机制族 | 版本 | dev | 线上Δ | 方向 |
|---|---|---|---|---|
| **分支内改动**（改文本/段内标签，**不改哪支服务哪段**）| v048 | −0.077 | −0.00058 | ✅ |
| | v051 | −0.180 | −0.00118 | ✅ |
| **路由/选源**（改哪支服务哪段）| v052 | −0.509 | **+0.00165** | ❌ |
| | v053 | −0.453 | **+0.00065** | ❌ |
| | v054 | −0.242 | **+0.00202** | ❌ |

**分支内族 2/2 方向一致，迁移率 0.00753 / 0.00656（均值 0.00705）；
路由族 0/3 —— 三次全部反向。**

### 对 v063 的影响

v063 最大单项成分是**路由层 −0.288**，正属 0/3 那族。三种情形：

| 情形 | 预测 |
|---|---|
| 路由层按分支内率兑现 | 0.14105 |
| 路由层完全不兑现 | 0.14308 |
| 路由层像历史一样反向 | **可能劣于 v051** |

**我此前报的 0.14116 隐含了「路由层会正常兑现」这个假设，而历史证据是 0/3。**

### v064：纯分支内版本（不动路由）

拆分与短段改写**都是分支内改动**（只改所选分支输出里的说话人标签，
不改哪支服务哪段）→ 属 2/2 那族。故造一个**不动 turn_rate** 的版本：

**产物** `submit/submission_v064.json`（4988 条 / 394 段 / `a89f7d85`）：
路由与 v051 **完全一致**（CAM 仍 132 段），同位置记录 4988/4988，
仅 **117 条说话人不同**、文本零差异 —— **纯分支内改动**。

dev **−0.381、四关全过**；按分支内族迁移率 0.00705 折算 → **预测 0.14239**。

### 修正后的推荐

| 序 | 版本 | 机制族 | 族战绩 | dev | 预测 |
|---|---|---|---|---|---|
| **1** | **v064** | **纯分支内** | **2/2 ✅** | −0.381 | **0.14239**（高置信）|
| 2 | v063 | 分支内 + 路由 | 混合 | −0.571 | 0.14105 ~ 0.14308（高方差）|
| 3 | v056 | 纯分支内（仅拆分）| 2/2 ✅ | −0.129 | 0.14416 |

**v064 与 v063 是一对刻意设计的对照**：唯一差别就是动不动 turn_rate。
两个都投，回执直接判定「路由族是否仍然反向」这个悬案 ——
若 v063 > v064，说明路由族的诅咒解除；若 v063 < v064，则确认路由轴线上不可用。

### ⚠️ 再更正：v064 的嵌套数比样本内差得多，对比反转

上一节按「机制族战绩」把 v064 排到首推，但**没有给它做嵌套**。补做后：

    v064 (mw=3, τ=0.45, n≤3, 路由不动) 样本内 -0.381
    各折选中 [(3,0.5,3), (3,0.45,4), (3,0.45,3)×3]
    嵌套 Δ -0.195   触发 16   改善 9/恶化 7
    [1]OK  [2]剔top3 +0.031 **FAIL**  [3]P=87.1% OK  [4]OK

**按分支内族迁移率 0.00705 折算 → 0.14369**（不是上一节写的 0.14239）。

### 两个候选的诚实对比

| | 嵌套 Δ | 四关 | 机制族 | 预测 |
|---|---|---|---|---|
| **v063** | **−0.571** | **全过** ✅ | 混合（含 0/3 族的路由层）| 0.14105 ~ 0.14308 |
| v064 | −0.195 | **挂第 2 关** | 纯 2/2 族 | 0.14369 |

**v063 在 dev 的每一项上都更好**（嵌套幅度大 2.9 倍、四关全过 vs 挂关），
唯一劣势是机制族。而**第 2 关历来是最强的线上预测器**（5/5 说对方向）。

→ **两个都投**（§104 下零下行）。推荐 **v063 优先**：
它是唯一「嵌套后仍四关全过」的候选，这个性质在历史上比族归属更能预测线上。
v064 作为族对照跟投，回执可判定路由族诅咒是否解除。

**教训（第二次犯同类错）**：我上一节仅凭「机制族战绩」就重排了推荐顺序，
却没先给新候选做嵌套 —— **换了个理由跳过验证，本质上仍是 gate2-not-overridable 那个毛病。**
任何候选进推荐位之前必须先过嵌套，无论它的先验多有说服力。

### 最后一层：上游 CAM++ 分离参数也确认在最优

`min=3/max=7/merge_thr=0.70` 是 **v002（7 月）** 定的，而 `diarize.py` 的 CLI 默认是
`min=2/max=6`，ref 人数分布也是 2–6 —— 看起来 `min_spk=3` 是个可疑的硬约束
（对那 10% 的双人段强制至少 3 人）。

先修好 GPU 上的分离 pipeline（`pip install funasr` 解决 `modelscope[audio]` 缺失），
再验证「改分离参数不改切分」（`spk_relabel` 保切分 106/106，切分来自 fun-asr）→ 扫参可行且便宜。

| 配置 | CAM 分支（relabel 后）| 人数分布 |
|---|---|---|
| **min3/max7/thr0.70（现行）** | **21.818%** | — |
| min2/max6/thr0.70 | **23.808%（+1.990）** | {2:31, 3:40, 4:21, 5:14} |

**放宽 `min_spk` 到 2 明显更差**（改善 31 / 恶化 42）：人数分布严重左偏，
说明**下界约束是防欠聚类的有用先验，不是错误的默认值**。

→ **连上游分离参数也在最优。全链路（分离 → 文本 → 归属 → 路由 → 后处理）
所有可调标量至此全部扫完。**

## 本日收官（最终）

**v063** = 最终首推：dev 四参数嵌套 **−0.571**、**唯一嵌套后仍四关全过**的候选。

有效改动共四项，全来自「继承来的默认值从没被单独扫过」：
`turn_rate 0.08→0.05`（−0.288）、拆分加「人数≤3」（−0.314）、
`max_words 5→3`（−0.077）、层间联合标定（−0.160）。

确认惰性或已最优的：word_arb 参数与 `MIN_SEG_SEC`、pick 其余四参数、
`spk_relabel` 的 `SPLIT_THR`/`MARGIN`/`WIN_SEC`、`spk_reassign` 全部参数、
拆分内部常数与分支特异阈值、**CAM++ 的 `min_spk`**。

六个候选已通过与线上已验证 v051 的同构审计（字段/类型/session 集/时间/排序/命名/空文本/最小段数），
不会因格式浪费额度。

## 🔧 格式修复（用户发现）：v056+ 与历次提交不同格式

用户指出「v063 的格式跟之前的不太一样」。核盘后确认**两处实质差异**：

| | 历次提交 v030–v054 | 我 8/27 造的 v056–v064 |
|---|---|---|
| JSON 排版 | `indent=2` 缩进 | **紧凑单行** |
| 说话人标签 | 仅 `spk1`–`spk7` | **含 `spk1_b`/`spk2_b`/`spk3_b`** |

**根因**：`merge_submit.py` 写文件带 `indent=2`，但我在 merge 之后追加的「拆分」那一步
用的是自写脚本，直接 `json.dump(...)` 没带 `indent`；`_b` 后缀则是拆分机制给新说话人起的名。

⚠️ **我此前的「同构审计」漏掉了这两项** —— 那次只查了字段集/类型/session 集/时间/排序/
空文本/最小段数，标签检查写的是 `startswith('spk')`，`spk1_b` 恰好通过。**审计写得太松。**

### 风险评估与修复

对 tcpWER **本身无害**（该指标对 hyp 说话人做全局最优置换映射，标签叫什么不影响得分）；
且 v053 含 `spk1_s` 标签并已正常评分（0.14572），说明平台不至于拒收。
但既然修复零成本，就按历史惯例统一，消除平台校验层面的未知风险。

**修复**：`spkN_b` → 该 session 内下一个空闲的 `spkM`；写文件改回 `indent=2`。

**等价性已逐版验证**（10 个候选全部通过）：
**说话人分组**（哪些段共享同一说话人，与标签名无关）与**逐段内容**完全相同，
仅标签名与排版变化 → 分数不受影响。

### 新指纹（旧指纹作废）

| 版本 | 新指纹 | 记录 |
|---|---|---|
| **v063** | **`6a237aa5`** | 5180 |
| v062 | `e827c184` | 5303 |
| v064 | `74f30fd8` | 4988 |
| v060 | `267b9b0d` | 5303 |
| v059 | `cc0e94ef` | 5303 |
| v056 | `2fbd093e` | 4988 |

`submit/` 与 `submit/archive/` 两处副本均已同步。

**教训：格式审计必须用「与已线上验证产物逐项比对」，不能用自己写的宽松规则。**
正确做法是拿 v051（已验证）当基准，比键序、排版、标签正则、记录结构，而不是 `startswith`。

## 📊 v063 线上回执 = 0.14443（新最佳，但兑现率仅 16%）

| | |
|---|---|
| 线上 | **0.14443**（vs v051 0.14507，**Δ−0.00064，新最佳**）|
| 预测 | 0.14116 → 误差 **+0.00327** |
| 反推本地 | **−0.093 点**（dev 嵌套预测 −0.571）|
| **兑现率** | **16%** |

排名：仍稳在第 3（第 4 名 0.14662）。距榜首 0.14185 还差 **0.00258**。

### 这个 16% 说明什么

v063 的成分：路由层 −0.288（历史 0/3 族）+ 分支内 −0.283（历史 2/2 族）。

三种可能，**必须用 v064 这个对照来区分**：

| 假设 | 含义 | v064 预期 |
|---|---|---|
| A. 路由层负兑现，分支内正常 | 分支内约 −0.283×0.7≈−0.2 点被路由层吃掉大半 | v064 应**明显好于** v063 |
| B. 两层都大幅缩水 | dev→线上整体迁移率被高估（不只路由族）| v064 与 v063 **相近** |
| C. 拆分机制本身不转移 | 新族（拆分）线上无效 | v064 也只有微幅改善 |

**v064 = 完全相同的配方但不动 turn_rate**（纯分支内）。它与 v063 是单变量对照，
回执直接判定假设 A/B/C —— 这是今天剩下 2 次额度里最有信息量的一投。

### 迁移率需要重标定

此前用的 0.00685/点 是从 v048/v051（分支内、小幅改动）标定的。
v063 实测 0.00064/0.571 = **0.00112/点**，仅为其 **16%**。

⚠️ 但 v063 是混合族，不能直接用它重标定分支内族的迁移率。
**必须等 v064 的回执才能拆开。**

### 🔬 路由税量化：两条独立路径给出同一个数

| 来源 | 数值 |
|---|---|
| v063 残差（实测总计 −0.00064 减去分支内应得 −0.00200）| **+0.00136** |
| 三次纯路由提交的线上Δ均值（v052 +0.00165 / v053 +0.00065 / v054 +0.00202）| **+0.00144** |

**两个独立算法几乎给出同一个数。** 结论：

> **路由类改动在线上带来近乎恒定的 +0.0014 惩罚，与 dev 预测的收益大小无关。**
> 四次观测全部一致（三次单独 + 一次作为残差）。

这解释了 v063 只兑现 16%：分支内那部分正常工作了（约 −0.0020），
但被路由层的 +0.0014 吃掉了七成。

### 修正后的预测模型

    线上Δ = (分支内 dev 点数) × 0.00705 + (若改了路由: +0.0014)

**v064 预测**：`0.14507 + (−0.195 × 0.00705) = 0.14370` —— 应优于 v063 的 0.14443。

⚠️ 该模型仍待 v064 检验。若 v064 落在 0.1437 附近 → 模型成立，
后续所有候选**一律不碰路由**；若 v064 ≈ v063 或更差 → 模型错，需重审嵌套流程。

### 冲榜一还需要多少

榜首 0.14185，我们 0.14443，差 **0.00258**。按分支内族迁移率 0.00705：
需要 **0.37 个分支内 dev 点**（在 v064 的 −0.195 之外**再多 0.37 点**）。

而分支内轴的标量已全部扫尽 —— **这个量级不可能靠调参拿到，必须有新机制。**
诚实讲：以现有候选，第 3 名可保，榜一够不到。

## 🔴🔴 v064 = 0.14600 —— 路由税模型被推翻，方向完全相反

三版构成**干净的单变量阶梯**（只差两个开关）：

| 版本 | 配置 | 线上 | 增量 |
|---|---|---|---|
| v051 | 基线（路由 0.08、短段 5 词、无拆分）| 0.14507 | — |
| v064 | v051 **+ 分支内**（短段 3 词 + 拆分）| **0.14600** | **+0.00093** ❌ |
| v063 | v064 **+ 路由 0.08→0.05** | **0.14443** | **−0.00157** ✅ |

### 我的模型完全说反了

上一节我用两条独立路径算出「路由税 +0.0014」，并据此预测：
路由有害、分支内有益、v064 应优于 v063（预测 0.14370）。

**实测 v064 = 0.14600，比 v063 差 0.00157，也比 v051 差 0.00093。**

真相恰好相反：
- **分支内改动（短段 3 词 + 拆分）线上 +0.00093 —— 有害**
- **路由改动（turn_rate 0.05）线上 −0.00157 —— 有益**

### 为什么「路由税」会算错

那个 +0.00136 残差是**假设「分支内按 0.00705 正常兑现」倒推出来的**。
一旦这个假设错了（分支内实际是有害的），残差就没有意义 ——
我把「分支内有害」的部分错误地记到了路由头上。

**教训：残差法只有在被减去的那一项独立可信时才成立。**
我用一个未经检验的假设去归因，得到的数字再漂亮（两条路径吻合到 +0.00136 vs +0.00144）
也只是**同一个错误假设的两次复现**，不是交叉验证。

⚠️ 这是本项目第二次栽在「看起来交叉验证、实则同源」上
（第一次是 [[gate2-not-overridable]] 里「8 轮留出取均值」——同一批幸运段反复出现）。

### 直接推论：v065 = 只改路由

若两个开关的效应可加：**只改路由、不动分支内** 应得
`0.14507 − 0.00157 = **0.14350**`，优于当前最佳 v063 的 0.14443。

**产物** `submit/submission_v065.json`（5186 条 / 394 段 / `e78bba70`）：
v051 链路，唯一改动 `turn_rate 0.08→0.05`；与 v051 同位置记录
**说话人与文本零差异**（差异全来自分支切换带来的切分变化），无退化 session。

距榜首 0.14185 预计还差 0.00165。

### 关键校准更正：不是「路由族不转移」，是「复杂机制不转移」

| 机制 | dev | 线上 | 判定 |
|---|---|---|---|
| **路由阈值**（简单确定性规则）| −0.288 | **−0.00157** | **转移，0.00545/点** ✅ |
| 拆分 + 短段改写（段内标签改写）| −0.195 | +0.00093 | 方向**反** ❌ |
| v052 学习式覆盖 / v053 分支整体替换 / v054 融合路由 | 负 | 全部正 | 不转移 ❌ |

**我此前把「路由族 0/3」当结论是错的** —— 那三次都是**复杂机制**
（学习模型、整体替换、stacking 覆盖）。**简单阈值规则转移得很好（55% 兑现率）。**

真正的分界不是「路由 vs 分支内」，而是 **「简单确定性规则 vs 复杂/学习机制」**。
这条线同时解释了历史上全部 8 次回执。

### pick 四参数联合扫描（此前只逐个扫过）

在 `turn_rate=0.05` 下扫 `moss_max_spk × cam_min_spk × cam_over_spk × cam_eq5_moss_min`
共 36 组合（34 个有改动）：**负收益 0 个**，最好的也只是 +0.067。

→ **默认四参数联合也最优。路由轴的规则空间确认挖尽，v065 即该轴最优点。**

## 今日收官

**线上最佳：v063 = 0.14443**（新最佳，rank 3 稳固；第 4 名 0.14662）。

三次提交换来一次干净的单变量拆解：
- 路由 `turn_rate 0.05` **有益 −0.00157**
- 分支内（拆分 + 短段 3 词）**有害 +0.00093**

**明日第一投：v065**（`e78bba70`）= v051 + 仅路由 0.05，预测 **0.14350**。
若兑现，距榜首 0.14185 剩 0.00165。

### 今天犯的两个错（都已更正并落盘）

1. **格式**：v056+ 用了紧凑 JSON 与 `spkN_b` 标签，与历次提交不一致 —— 用户发现。
   根因是我的同构审计写成 `startswith('spk')`，太松。已全部规范化并验证语义等价。
2. **归因**：用「残差法」算出「路由税 +0.0014」，两条路径吻合到 +0.00136 vs +0.00144，
   但**被减去的那一项（分支内按 0.00705 兑现）本身是错的假设** ——
   两次吻合只是同一个错误假设的复现，不是交叉验证。v064 的回执直接推翻了它。

### 顺「简单确定性规则」这条线继续挖：normalize 与语气词

既然分界是「简单规则转移、复杂机制不转移」，而 **v028（输出层规范化）是历史单次收益最大的版本
（−0.00183）**，就该重查这一族。

**① `normalize` 本身工作正常。** 字符级频率比对：ref 从不写「哦」（0 次）、只写「噢」（53 次），
hyp 反过来 —— 而最终产物里「哦/哎/诶」已全部为 0，规则确实生效了。

**② 但比对暴露了新现象：语气词过量。**

| 字 | v065 | dev ref | 比值 |
|---|---|---|---|
| 嗯 | 526 | 288 | 1.8× |
| 呃 | 88 | 48 | 1.8× |
| 唉 | 67 | 36 | 1.9× |
| 噢 | 77 | 53 | 1.5× |

看起来像纯插入。测「删语气词」这个简单确定性规则：

| 规则 | 删词 | Δ点 | 改善/恶化 |
|---|---|---|---|
| 删全部「嗯」| 172 | **+0.427** | 8/56 |
| 删全部「嗯呃噢唉」| 226 | **+0.555** | 7/63 |
| 只删纯语气词段 | 52 | +0.021 | 10/16 |
| 只删段首语气词 | 161 | +0.375 | 7/54 |

**全线有害。** → 那些「多出来」的语气词**在 collar 5 秒内是能对上 ref 的**，不是插入；
删掉反而制造删除错误。**词频计数差 ≠ 插入**，这是个容易误判的陷阱。

→ **简单确定性规则这一族至此也挖尽**（路由规则 + 参数联合 + normalize + 语气词）。

### 简单规则族的最后两项：重复与重叠 —— 均无异常

| 指标 | hyp | dev ref | 判定 |
|---|---|---|---|
| 段内 3-gram 重复 ≥3 次 | 5 段 | 3 段 | 正常（无 ASR 循环失效）|
| 相邻段文本完全相同 | 2 段 | 13 段 | 正常（hyp 反而更少）|
| 相邻段时间重叠率 | 20.6% | 21.7% | 正常（几乎一致）|

→ **无去重、无重叠修复的余地。简单确定性规则族全部挖尽：**
路由规则 + pick 参数联合 + normalize + 语气词 + 去重 + 重叠。

## 2026-08-27 最终状态

**线上最佳 v063 = 0.14443**（rank 3 稳固，第 4 名 0.14662）。今日额度 3/3 用尽。

**明日第一投 v065**（`e78bba70`）= v051 + 仅路由 0.05，预测 **0.14350**。

### 今日三次提交的净产出

一次干净的单变量拆解（v051 → v064 → v063 只差两个开关）：

| 开关 | 线上 |
|---|---|
| 路由 `turn_rate 0.05` | **−0.00157 有益** |
| 分支内（拆分 + 短段 3 词）| **+0.00093 有害** |

以及一条**贯穿全部 8 次回执的分界线**：
**简单确定性规则转移（55% 兑现），复杂/学习机制不转移（全部反向）。**

### 缺口

v065 若兑现，距榜首 0.14185 剩 **0.00165** ≈ **0.30 个 dev 点**。
简单规则族已挖尽，**需要一个此前未想到的确定性机制** —— 今天没找到。

### 分词口径审计（此前从未系统查过）—— 完全干净

赛题明写「每个字/英文单词中间用空格分割，英文全部小写」。逐项比对：

| 指标 | dev ref | v065（最终产物）|
|---|---|---|
| 多字汉词 | 0 | **0** |
| 英文含大写 | 0 | **0** |
| 阿拉伯数字 | 0 | **0**（pre-norm 有 1 个，normalize 已转换）|
| 非常规 token | 无 | **无** |

→ **输出约定与 ref 逐项一致，排除一整类系统性错误。**

至此 2026-08-27 的搜索完全收敛：简单确定性规则族的**每一项**都已检验
（路由规则、pick 参数联合、normalize、语气词、去重、重叠、分词口径）。

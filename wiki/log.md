# Wiki 时间线 + sync 水位

> 记录 wiki 的查询/沉淀/体检/方向选择动作，以及对账水位。
> 每轮收尾读 last-synced-version vs SCORES.md 最新版本号；对不上 = 有迭代漏了 ingest，先补录再回用户请求。

**last-synced-version**: **v067（2026-08-28）**。SCORES.md 排行榜最新两行 = v067 (0.14952) / v066 (0.14603)，
与本水位对得上。**当前最佳 = v065 = 0.14354**（线上，指纹 e78bba70），榜首 0.14185，缺口 **0.00169**
→ 按迁移率 0.00685 折算 **所需 0.247 本地点**（此口径截至 2026-08-28，榜首或最佳一变必须重算）。
⚠️ `submit/archive/` 里混有**未提交**的 v068，**以 `online_ledger.md` 为准**，archive 不能当「已提交」证据。

<details><summary>历史水位（v037，2026-08-13）</summary>

🏆 **破榜首**：v037 线上 **0.14857**，超当时榜首 0.15023 共 **0.00166（1.10%）**。
配方 = 基座插值 soup（0.75×v031 + 0.25×未微调基座）**+ 归属轴 CAM 源纯重分配**。
归因干净（v036 单独 0.15137 差于 v031；两版仅差 29 条 speaker）→ **归属轴独立贡献 −0.00280**。
`last-synced-calibration` = v037。

</details>


## 时间线

### 2026-08-29
- `ingest` | **判官轴（撤回侧）五信号族全封**：文本派生 32~39% / MOSS 声学似然 34% / 结构性 71% 但触发面 5~15 段 / wav 物理层 ≤0 且与声学共线 / ERes2NetV2 嵌入**逻辑死**（`word_arb.py:258` 只切时间窗不动时间戳 ⇒ `emb(src)≡emb(arb)`，边际信号恒为 0）。详见 `logs/2026-08-29.md` §五、七、八。
- `ingest` | **v065 链路认亲坐实**：`submission_v065.json` = `merge_submit(output_test_v065_norm)` **394/394 全等**；`normalize_output.py` 是**纯文本归一化**（改 85 段 `words`，时间轴指纹 394/394 不变）。
- `ingest` | **统一口径消融台账**（dev 106 段全覆盖，24 个目录同表，`tmp/ablate.py`）。空间在结构上分解为「时间轴 × 文本」两正交轴（11 时间轴 / 20 文本）。同轴独立复现：word_arb −0.931 点、**捕获率 68.6%**；FireRed 文本单独 19.4220% = **比 MOSS 文本差 1.682 点**（只配当第二候选）。
- `lint` | **CAM 段数轴的 x 坐标不可复现**。`logs/2026-08-28.md` 内部两套计数（第 26 行 v065=65 / 第 61 行 v065=83）；实测（v068 为锚）v051=99 / v065=50 / v066=83 / v067=40，与声称处处差 **33**，回代一个 33 段集合 S 可精确复原全部声称值，且 S 在两次独立减法下逐段相同。分支产物未留存 ⇒ 不可判定。**`insights/cam-count-axis-has-optimum.md` 的「曲线极小值」应降级为「四个实测点中 v065 最优」**（线上四点事实不受影响）。
- `lint` | **`relabel-family-worth-031.md:57` 的建议已失去依据，建议摘除**：该页「归属轴唯一有意义的方向是重切时间轴」推自**已被 `namespace-artifact-reopens-attribution` 作废的 0.31 点**；本次实测 `_cv` vs `_cv_orc_spk` **106/106 段边界完全相同、仅标签不同**即拿到 −4.381 点 ⇒ **边界从来不是这条轴的约束**。（诚实上界仍取 hyp 命名空间的 −1.96 点；该轴因「基线长段已 98.1% 正确」保持封死。）
- `lint` | 「目录名不是契约」第 5、6 次：`logs/2026-08-29.md` 引用的 `cvfinal` / `truecam_wa` / `truecam_re` **三个目录都不存在**（实为 `_cv_final`，数字 15.7505% 吻合）。另 `tmp/score_dir.py:14` 会把 ref 裁剪成目录内 session ⇒ 段数不全的目录分数不可比。
- `ingest` | **结构瑕疵不是得分项（闭环）**：v065 产物有 2 条空 `words` / 36 对同人自重叠 17.4s / session 046 塌成单说话人。dev 侧前两项**无实例**（不可本地验证），自重叠修完重测 **15.5962% / 3033 errors 逐位不变** —— **collar = 5 秒，亚秒级边界瑕疵不进计分**。
- `select` | 三条轴（文本 / 归属 / 时间轴选源）**各自在其机制族内均已挖尽**，台账未翻出漏掉的收益。**无可提交新版本**，v065 = 0.14354 维持最佳。便宜档剩余动作只有「重建可复现的纯 MOSS test 时间轴」以恢复路由实验的可对账性。
- `ingest` | **🔑 找回「已丢失」的 test 分支产物 —— 此前判死是搜索错误**：历史分支在 `baseline/output/` 下用 **`test_*` 前缀**（此前只 glob 了 `output_test*`）。`test_cam_wa` = 历史 test CAM 分支（v065 的 82 个路由段中 **64 段逐条全等**、时间轴命中 **83** 段）；`test_v051_moss` = MOSS 分支（与 v068 时间轴同 393/394）。**选源轴在 test 上从此完全可控。**
- `lint` | **「83 vs 50」结案（撤销上面那条「不可判定」）**：v065 与全 MOSS 差异 **82 段 = 50 段时间轴 + 32 段仅文本**；日志的「83」数的是 **CAM 时间轴命中数**，第 27 行的「50」是 **v065↔v068 时间轴差异数** —— **两个都对，量的是不同东西**，那 33 段集合 S = 仅文本差异组。验证：出货规则套在 (MOSS=v068, CAM=另一候选) 对上预测 94 段，**v065 实际 82 段是其完美子集**（v065有/预测没有 = 0）。
- `lint` | **🔑 第二处口径错误：忠实链路取了 v048 期的 MOSS，所有数字悲观 0.19 点**。`_cv_re` 与 `_cv_mb6` 段数(1519)/时间边界(0/106 不同)/说话人数(382) 全同，只差 7 段标签 + 81 段文本（= 词级仲裁变体）。认亲：**v051 期出货产物（`_camwa_final`/`_camwa_pick`）的 MOSS 侧文本与 `_cv_mb6` 一致，v048 期的（`_cv_final`/`_cv_pick`）与 `_cv_re` 一致** ⇒ 出货链路自 v051 起已是 `_cv_mb6`。修正后：全MOSS 16.8098% / 出货规则 **15.7556%** / oracle **14.3210%**，与 bench 口径 `_cv_final` 15.7505% 终于对上（差 0.005）。⚠️ **这 0.19 不是可得收益，是基线修正。**
- `ingest` | **`turn_rate` 是量化的**：session 约 45 秒、切换次数为整数 ⇒ 1次≈0.022/2次≈0.044/3次≈0.067。**`turn_rate<=0.05` 的真实含义是「MOSS 只找到 ≤2 次说话人切换」**。正确参数化是切换次数（改用次数重跑 180 组网格，结论不变）。
- `select` | **选源轴九条机制全否**（详见 `logs/2026-08-29.md` §十E）。两条曾四关全过但都被推翻：① **`nspk(MOSS)≠nspk(CAM)`→CAM**（零参数，污染对上 Δ−0.6016、四关全过）在**未污染对上 +3.66 点、三关全挂**，且线上独立同向（v051 路由132段 0.14507 劣于 v065 的83段 0.14354，「多路由」在线上是坏的）⇒ **污染分支幻觉**；② **MOSS 变体换 `_cv_re`→`_cv_mb6`**（Δ−0.1903、四关全过）⇒ **出货链路早已入账**。
- `insight` | **🔑 选源轴在 dev 上无法验证**：session 级路由改动最多触及十几段（dev 106 段、CAM 真赢 32 段），而 bench 第1关要求触发面≥8、第2关要求剔 top3 后仍为收益 —— **这两关是为触及数百段的文本级机制校准的**。⇒ **选源轴是被测量分辨率卡住，不是被机制卡住**，唯一验证途径是线上额度。
- `ingest` | **归档重组框架**（`tmp/recomb.py`/`tmp/emit.py`）：同族版本在共用时间轴的 session 上文本 **2223/2223 一致** ⇒ 记录集由「选哪个候选」唯一决定，跨版本重组无损。以 v051（全候选0）为截距 c=0.14507 解出 δ(组33→1)=+0.00096、δ(组39→1)+δ(组4→2)=+0.00502，**v065 全部优势 −0.00153 集中在 4 组 49 段**。6 个探针候选经**独立重建 CAM 投票 14/15 反对**后全部删除，**省下 6 个额度**。
- `ingest` | test 全 394 段独立 CAM 已重建（`baseline/output_test_cam_rebuild`，纯 CPU 2 分钟，配方与 dev `cam_clean2n` 逐字相同）。与历史 `test_cam_wa` 认亲仅 4.1% ⇒ 是**独立第三份意见**，不是复制品。历史从未路由过的 218 段上出货规则只触发 1 段 ⇒ 不开新地。
- `ingest` | **🔑 忠实路由构造器建成**：MOSS=v068 + CAM=`test_cam_wa` + 出货规则，预测路由集与 v065 实际 82 段**双向零差异** ⇒ test 侧路由可任意构造且完全忠实。
- `ingest` | **顶点重定位到 ~92 段**：改用**三个同 CAM 构造（v053 后）的线上点** v053@132=0.14572 / v065@82=0.14354 / v066@50=0.14603 二次拟合 ⇒ 顶点 **x=92.3, y=0.14338**（优于 v065 共 0.00016），与上一会话四点拟合的 89.7 **独立吻合**（那次 x 坐标还不可复算）。有用区间仅 86~96 段。`turn_rate 0.05→0.06` 恰好给 **92 段、纯超集（移除 0）**。⇒ 候选 **v078**（`5f44931545`）= 本日唯一正预期，待人工提交；v079（86 段）夹住顶点。
- `select` | **否掉 v076**（58 段，落在 50/82 之间劣区，拟合预测 ~0.1451 退步）。删除 v069~v074、v077（有明确反证）。
- `insight` | **文本轴 token 层封死**：dev 989 个替换对集中在语气词互混（嗯↔呃↔啊 共 56）与同音代词（他/她/它 共 46、得/的 15），但**每个 hyp token 对齐到自身都是多数**（`呃` 最接近：46% 自身 vs 40% 对齐到 `嗯`，映射仍净亏 3；`他` 82% 自身）⇒ **无任何确定性单 token 映射可盈利**。他/她/它 读音完全相同，要分辨必须靠语义，唯一工具 LLM 合规未确认且七月已证伪。

- `insight` | **🔑 线上分对 session 严格可加 ⇒ 额度可做精确分组测试**。tcpWER=Σ错误/Σ长度 且 ref 长度与选哪个分支无关 ⇒ 「把某批 session 换分支」的贡献可由两次提交**相减精确得到，不是估计**。据此产出 **7 级严格嵌套阶梯**（v080@85 ⊂ v081@88 ⊂ v078@92 ⊂ v083@94 ⊂ v084@97 ⊂ v085@100 ⊂ v086@114，全为 v065 路由集的纯超集、词量 67763~67807 稳定），按 MOSS `turn_rate` 升序加入。**最终组合 = v065 + 所有 Δ<0 的区间，分数 = 0.14354 + Σ(负 Δ)，精确可算**；7 个区间只要有一个为负就严格优于 v065。额度账：7 级 + 1 组合 = 8 个，余 4。差分协议见 `logs/2026-08-29.md` §J。
- `select` | 删除负预期候选（v069~v074 重组探针 / v077 污染幻觉 / v079 破坏嵌套 / v076 落劣区），**用同样的额度换成正预期 + 精确归因的阶梯**。

### 2026-08-28
- `ingest` | **v065 = 0.14354 线上新最佳**，预报误差 **0.00004**（全项目最准）。单变量拆解经三版互证：路由 turn_rate 0.05 单独 −0.00153、分支内标签改写单独 +0.00093（有害）。
- `ingest` | **v067 = 0.14952，预报误差 +0.00835（本项目最大）**。「少用 CAM」线性外推破产 → 三点二次拟合顶点 x=89.7/y=0.14350，**v065 即极小值**。落页 `insights/cam-count-axis-has-optimum.md`。
- `select` | **封死「CAM 段数」轴**（顶点与 v065 差 0.00004，不存在更优操作点）。**连带撤回 v066 / v068**：拟合预测 0.14489 / 0.15058，均差于 v065，**不提交**。
- `lint` | 补录 SCORES.md 分数表续（v054–v067）——此前 v054 起只落在 ledger/logs，违反「SCORES 是唯一 durable 分数源」。标注 `submit/archive/` 含未提交的 v066/v068，archive 不再可当「已提交」证据。
- `query` | 文本轴查重：X13（换 Qwen3-ASR，劣 1.74 点）、v029（fun-asr 第三源，劣 1.558 点）、n-best（oracle 扩 1.093 但捕获退 0.186）**三条机制死，永久**；word_arb 参数已挖尽。
- `ingest` | **重测当前链路两轴空间**（`/tmp/chain_0.05`，scorer 用 `_cv_final`=15.7505% 校准）：文本 oracle **10.9220%** → 空间 **4.757 点**；归属 oracle **12.3515%** → 空间 **3.327 点**。文本轴自 08-05 的 5.563 点只被吃掉 0.8 点。
- `lint` | 🔴 **发现结构性缺陷：requirement-relative 封条从不过期**。`SCORES.md:326`「仲裁 family 上界 1.732 < 所需 2.03」—— 需求今天是 **0.247 点**，缩了 8.2×，封条失效。落页 `insights/requirement-relative-verdicts-expire.md`。
- `ingest` | **仲裁 family 探针（零成本，`_cv_fr` 已在盘）**：完美裁判上界 **16.8869%** vs 当前 `_cv_wa` 16.9383% = **仅 0.051 点 ≪ 需求 0.247**。三源平局率从 08-05 的 66.1% 升到 **93.9%**（`_cv_wa`/`_cv_re` 文本逐段相同 → 实为 2 源）。**改判机制死，永久封死。**
- `insight` | 文本轴结构量化：总空间 4.757 点中，「换源/仲裁」只够得着 **0.051 点(1.1%)**，**98.9% 落在三源一起错的平局段** → 只能靠「修所有源共同的错」。库内唯一该类机制 `llm_postprocess.py` 被 **Q3 合规未确认**卡住（组委会问题，非技术问题）。
- `lint` | 🔴 **推翻上面两条（第 31/32 行）的前提**：那次探针把 **`_cv_wa` 当成了「当前」基线和源**，但 `_cv_wa` 是 **word_arb 的输出不是输入**（它与 `_cv_re` 的 words 逐段 **0/1519** 差异）。真正的换前源是 **`_cv`**（对 `_cv_wa` 差 416 段 = 记录里 M=0 的换段数）。用正确的源重测：2 源 oracle = **16.3881%**（不是 16.8869%），出货基线 = **16.8098%**（不是 16.9383%）。**「仲裁 family 永久封死」这个封条作废**，它证的只是「在已仲裁产物上再改判」几乎没空间，不是「第一次就判对」没空间。→ 第三次踩「目录名不是契约」。
- `ingest` | **判官轴 headroom 重测 = 0.5245 点**（`oracle{MOSS, 出货仲裁}` 只撤有害换段：16.8098% → **16.2853%**）＝ 所需 0.247 点的 **2.1 倍**。341 次换段里**严格有害 88 / 有益 177 / 平局 76** → **病因是误换不是漏换**，推翻 `insights/word-arb-capture-headroom.md` 挂了半个月的「裁判太保守」解释（该页已修订）。
- `select` | **封死三个形式**：① 加宽判官上下文（`CTX_SEGS=1` → 17.7508%，**比 MOSS 源 17.7405% 还差**，起作用的是「恰好一段」的对齐尺度而非上下文量）；② 加弱源抬天花板（3/4 源使编辑距离代理**变好**而 tcpWER **变坏**：4018/16.3881 → 3960/16.5887 → 3906/16.6092 ⇒ **弱源只准当投票人，永不贡献文本**）；③ 文本一致性投票过滤（新 `baseline/src/arb_filter.py`，四个信号精度 **32~39%**，盈亏平衡需 ~50%，全部实测劣于不过滤）。
- `insight` | **轴开着但机制全枯**：0.5245 点空间形状对（纯撤回，不需新文本/新模型/GPU/联网），但四个独立信号收敛在 32~39% ⇒ 「文本一致性类信号整体够不到」，非四次运气不好。下一问不是「换什么特征」而是「有没有非 ref 信号能在这 341 段上做到 50% 精度」；若没有，该轴进**可达性探针门**待判队列。
- `lint` | 修 `bench.py` 过期常数：`LEADER` 0.142 → **0.14185**、`BEST_ONLINE` 0.14507（v051）→ **0.14354（v065）**，并把「所需 = (BEST_ONLINE−LEADER)/TRANSFER」的重算公式写进注释——这两个常数正是 requirement-relative 封条的分母，过期会把不够的方向判成够。

- `select` | ~~下一方向：**文本轴（4.757 点空间，需求仅占 5.2%）**，且需先跑探针收窄捕获率区间（悲观 2.1% → 0.036 点不够；历史 42% → 0.73 点为需求 3 倍）。当前最佳 v065 距榜首 0.14185 差 0.00169。~~ **← 同日由探针修正：选源路只值 0.051 点。归属轴 3.327 点成为唯一还开着的大空间。**（🔴 再修正：那道探针源认错了，选源/判官路实际值 **0.5245 点**；归属轴仍是最大空间但不再是「唯一开着的」。）
- `lint` | 🔴 **把作废落到页面上**（此前只落在时间线第 33 行）：`insights/requirement-relative-verdicts-expire.md` 的 frontmatter status 从「已闭环…探针当天就重新封死了它」改为「主旨成立、结局改判」，正文 4 处（0.051 结论 / 「这次是永久的」/ 文本轴 1.1% 结构 / 「方法论上这是一次成功」）加修订框；`wiki/index.md` 第 64 行同步。**新增探针纪律：盖封条前必须先证「输入 ≠ 输出」**——本例只需一行段级 diff（`_cv_wa` vs `_cv_re` = 0/1519 即暴露）。
- `query` | 判官轴续查：**「判官打分差」这个信号已挖尽**——`MARGIN` 全扫过（0.03 −0.036 / **0.05 −0.077** / 0.07 −0.051 / 0.10 +0.026 / 0.15 +0.108，见 `insights/word-arb-capture-headroom.md:43`），0.05 是峰，两侧都劣。⚠️ 同时**修正盈亏平衡线**：按误换/有益的**单段代价**折算（有益 177 段共 +1.4552 点 = 0.0082/段；有害 88 段共 −0.5245 点 = 0.0060/段），撤回一段的盈亏平衡精度是 **57.7%**，不是先前记的 ~50%。四个信号 32~39% ⇒ 差距比原判更大。

**last-synced-version**: v067

### 2026-08-13（🏆 破榜首 + 两条关卡修正）

- `ingest` | 🔴 **v034 线上 0.15532 倒退**（vs v031 +0.00436）。Model soup（800×2ep + 3000×2ep 等权平均）。
  价值在校准：Δholdout **+0.068 低于判据线 0.39**，我判「打平」，线上退 0.436 点 →
  **判据线管幅度不管符号**。
- `ingest` | 🔴 **v036 线上 0.15137**（vs v031 +0.00041）。基座插值 soup，Δholdout **−0.546、P=81.7%**，
  当日最强候选，**线上方向却反了**（倍率 −0.08x）——**本项目第一个方向判断失败的对**。
- `insight` | ⭐ **在验收集上择优 = 用掉它的预测力**。0.25 这个比例是在 holdout-24 上压过其它 soup
  变体选出的。按「是否在 holdout 择优」分组：**未择优 2/2 方向全对；已择优 1/2，抛硬币**。
  与 v032 同病。→ 落 [dev-online-transfer](insights/dev-online-transfer.md)。
- `ingest` | 🏆🏆🏆 **v037 线上 0.14857，破榜首 0.15023 共 0.00166（1.10%），本项目最佳**。
  = v036 基座插值 **+ 归属轴 CAM 源纯重分配**。**归因干净**：v036/v037 仅差 29 条 speaker →
  **归属轴独立贡献 −0.00280**，超过 v031 自己的历史最大单版增益（0.00477）的一半。
- `insight` | ⭐⭐⭐ **重尾关卡会漏报，「反向损失 = 0」是更可靠的正向信号**。
  归属轴机制当时被关卡判 FAIL、holdout-24 上完全不可见，我据此判期望 ≈0 —— **判错**。
  失效机制：**关卡量的是 dev 上收益是否集中，但该机制只作用于 CAM 源子群体，
  dev 只有 37 段（触发 2 个，看着集中）、test 有 ~128 段（触发 77 个，其实分散）**。
  → 用关卡前先问「该机制能作用的子群体在 dev 上有多少段」，<50 段时集中度读数不可信。
  四点一致：反向损失 0 的 v028/v037 都迁移良好，有变差段的 v027/v011 都翻车。
- `insight` | **基座插值有效的机制：「让分布贴近 ref」不是目标**。b25 的段数 12→10、切换率回落
  到 base 水平（更远离 ref），分数反而更好 → 微调多产的段是同一说话人内部伪切分，是净噪声。
  修正 D12 的隐含框架。
- `ingest` | ❌ 2026-08-13 上午六条探针全部关闭（归属轴纯重分配单独评估 / 语气词声学回填 oracle 为负 /
  字符残留仅 0.206 点 / 段内漏词 FireRed 反而少转 115 词 / 时间轴并集 +84 错误 / 仲裁轴
  完美再仲裁仅 2.3% 相对）。**注意：其中「归属轴纯重分配」的悲观结论已被 v037 推翻**（见上）。
- `select` | 下一步 = **① 提交 `submission_v035.json`**（v031 基座 + 同一归属轴，估 ~0.1482）；
  **② 归属轴既然成立，回头挖它的变体**（v027 的拆人分支曾被封死，但那是「拆人」不是「重分配」，
  现在有了正向证据，值得重扫 MARGIN / 把机制也用到 MOSS 分支）。
  ⚠️ 选型一律不得再用 holdout-24 择优，改用「反向损失 = 0 + 干净全 dev」。

### 2026-08-12 下半场（D11/D12/D13 全部跑完，D10–D14 收官）

- `ingest` | ❌ **D11 微调过程监控**：建起 `ft/eval_loss.py` + `ft/make_val_jsonl.py`（此前 `finetune.py` 零 eval、`sim_jsonl/holdout.jsonl` 是空文件）。
  ① 1ep（现成的 checkpoint-200）vs 2ep：每级差 0.250 点，但该对比判据线 **0.57 点** → 不可分辨。
  ② **验证 NLL 与线上反相关**：仿真验证集 120 段 + 真实 holdout-24 两套 NLL **都单调下降**（ckpt-1500 最低），线上排序却非单调（ckpt-400 0.15096 < base 0.15573 < ckpt-1500 0.15750）。
- `insight` | **teacher-forcing NLL 结构性看不见本赛的主误差**。它测「给定正确前缀猜下一个 token」，tcpWER 测「从零自由生成」，中间隔着 exposure bias；本赛主误差正是自由生成时的分割/归属漂移，teacher forcing 每步把漂移清零。**→ 微调选 checkpoint 只能用生成式指标。**
- `ingest` | ❌ **D12 仿真保真度**。发现新缺陷：**段内重复句占比 34.3%（真实 dev 仅 0.3%，单句最高重复 19 次）**。修了 `build_one` 的游标 bug（放不下的候选被永久消耗）→ 重复率 34.9%，**纹丝不动**，假设证否；真因结构性：dev 每个 (session,speaker) 池子中位仅 **4 条**，而 42s 对话每人需约 5 段。ss1.0 实测 15.138%（vs v031 14.728%）。
- `insight` | ⭐ **微调的转移函数：段时长可控，切换率完全不可控**。训练数据切换率 0.238→0.253，模型输出**钉死 0.140**（目标 0.194），每 session 段数钉死 12（目标 19）——新增的段全是同说话人内部切分。**→ 段塌缩是模型说话人分辨能力上限，不是数据分布问题，仿真数据这条轴够不到它。轴挖透。**
- `ingest` | ❌ **D13 model soup**（`sim_out` + `sim_out_3k` 均匀权重平均，`ft/soup.py`）。**不受 text-segmentation-coupling 封锁**（权重平均不产生新分割，机制不同）。raw MOSS **17.642→16.230（−1.411）**，但端到端 14.796% vs v031 14.728%（**+0.068，判据线 0.39，P(soup更优)=37.9% → 打平**）。
- `insight` | ⭐⭐⭐ **三次撞的是同一堵墙**：上游改进真实有效，但落在下游 `pick_ensemble` 已补偿的段上 → 归零。soup 的 −60 错误**全在 pick 切走的 10 段**（把 MOSS 从 12.823% 拉到 9.521%，而 CAM 在那儿本就是 9.576%，**追平而非超过**），保留的 14 段反而 +4。→ [downstream-compensation-absorbs-gains](insights/downstream-compensation-absorbs-gains.md)
- `select` | **D10–D14 五个方向全部试完，无一产出可提交版本，当前最佳仍是 v031 = 0.15096。** 下一轮靶子已量化：**pick 保留 MOSS 的 14 段 = 18.91%**，ins128/del222/sub137、**missed_spk 仅 6/54** → 说话人数找对了，错在**边界与词的归属**。⚠️ **距校准 5/3 已超期**，动手前先 calibrate。

### 2026-08-12（补录 v031–v033 + D14 证否 + 一条新定律）

- `ingest` | **v031 线上 0.15096，本项目最佳，距榜首仅 0.00073**。机制 = **仿真拼接微调 MOSS**（800 段由 dev train-82 单说话人片段跨 session 重组，画像对齐本赛：3–6 人 / 每 2.2s 换人 / 41.9s / 重叠 22%）+ v028 全链路，pick 阈值 0.10→0.08。
  🔴 **迁移率 233%** —— holdout-24 只测出 0.205 点、线上实得 0.477 点，**首次线上大幅超出本地预期**。
- `insight` | **仿真微调成立而 RAMC 微调失败，差别只在数据画像**。同配方同量级：RAMC +2.41（更差），仿真 −0.296（更好）。→ 数据质量 > 数据量，这条在 v032 上再次被验证。
- `ingest` | 🔴 **v032 线上 0.15879 倒退**（+0.00783）。**一次改了两个变量**（数据 800→3000 + 阈值 `cam_min_spk 4`），流程错误。
- `ingest` | **v033 线上 0.15750**，拆变量专用版（只改基座）。**归因：扩数据本身占倒退的 84%、阈值占 16%** → **「800→3000 扩数据」方向证否**。
- `insight` | **修正一条错误推论**：曾从 v032 判「holdout-24 无预测力」。v033 显示只改基座时 Δholdout-24 与 Δ线上**方向一致** —— 真因是**把 holdout-24 同时当调参集和验收集**（评测集污染），不是样本太小。
- `insight` | **评测分辨率取决于两版的相关性，不取决于评测集大小**。同一个全 dev：只差字符映射时判据线 **0.10 点**，换基座时 **1.13 点**，换整条 pipeline **1.95 点**。工具 `baseline/src/holdout_noise.py`（配对自助重采样）。
- `query` | **D14 查重**：`text-segmentation-coupling` §四留的唯一未验证变体（先选源、再在选定分割上做词级仲裁）。wiki 无前例，可跑。
- `ingest` | ❌ **D14 证否**。`word_arb.py` 源无关，零改动换成 CAM 两源（GPU 3090，201s / 全 dev）。CAM 分支 22.754%→**21.973%（−0.782，仲裁本身有效）**，但 **pick 真正选中的 37 段只有 −0.030（捕获率 2.1%，15 胜/18 负/4 平 = 随机）**，全链路 15.6991% vs v028 的 15.6425%。→ **discard**。
- `insight` | ⭐⭐ **新定律：捕获率是「源 headroom」的函数，不是系统常数**。同一裁判同一次运行：源 27.8% → 捕获 **77.3%**；源 12.9% → 捕获 **2.1%**（oracle 两边几乎相同）。死因结构性：**pick 挑走的正是 CAM 已经很好的段**，那里两候选声学近乎等价。
  ⚠️ 我跑 D14 前用「oracle 0.494 × 历史捕获率 46% = 预期 0.227 点」判它值得跑，**实测预期错了 20 倍**。已落 [capture-rate-scales-with-headroom](insights/capture-rate-scales-with-headroom.md)，并回改 `arbitration-signal-must-be-acoustic`（43.7% 是 MOSS 源口径）与 `text-segmentation-coupling` §四。
- `insight` | 副产物：仲裁把「MOSS分支 vs CAM 完美二选一」的 oracle 从 **0.730 扩到 1.054 点**（全 dev 口径）。**空间变大、闸门没变** —— 仍缺选源器，选源信号已连否四类。
- `select` | 下一候选 = **D11 微调过程监控**（验证 loss / early stopping）。依据：v031 证明仿真微调是活的主线，v032/v033 证明「加量」是死路 → 剩下的活路在**训练过程本身**而非数据规模。最便宜的第一步：800 条跑 1 epoch（13 分钟）与 2 epoch 对比，区分过训练 vs 产物噪声。
  ⚠️ **超期 5/3 未校准**，D11 出结果前建议先跑一次 calibrate 重分层。

### 2026-08-09（v028 破纪录 + 四条方向证伪 + 一次严重倒退）

- `ledger` | **v028 线上 = 0.15573，新最佳**。破 v026 的 0.15756 共 **0.00183**，距榜首 0.15023 差 **0.550 点**。
  **迁移率约 107%**（dev −0.278 点 → 线上 −0.183 点），是**首个两道正交关卡同时通过**的版本。
  机制 = 输出层规范化：把 ref 词表中出现 0 次的字符改写成 ref 写法（哦/喔→噢、哎/诶→唉、阿拉伯数字→中文单字）。
- `insight` | **「输出规范化」是一条此前完全没人看过的轴**。第一性原理复盘发现：所有已挖节点
  （FireRed 重转写 / 词级仲裁 / 归属改写 / 择优）**全都条件于 MOSS 的分割**，而**输出环节本身从没被审视**。
  ref 中出现 0 次的字符共 39 种 103 次 = **0.530 点必错**，与识别能力无关。
  **反向损失由构造保证为 0** —— 这是本项目唯一一类「不可能弄坏对的」改动。
- `ledger` | 🔴 **v030 线上 = 0.1737，本项目最严重的一次倒退**（vs v028 +0.01797，迁移率 **−216%**）。
  dev 14.810% 看着极好，**根因是 dev 数据泄露**：ck84 在 81 段 dev 上训练，dev 评测却用全 106 段。
- `insight` | 🔴 **关卡判 FAIL 时不应找理由绕过**。重尾关卡对 v028→v030 判 FAIL（−0.082），**预测正确**，
  当时被「v028 后处理抢功的假象」这个理由绕开了。而「base→ck84 关卡 PASS」不可作为反证 ——
  两者都在含训练数据的 dev 上评测，PASS 本身就含 memorization。
  → **在 dev 上微调再在 dev 上评测 = 循环论证，holdout 怎么切都防不住，因为它与训练集同源。**
- `ingest` | **D5.3 微调彻底封死**：v012（0.17023）/ v013（0.16872）/ v030（0.1737）**三次线上三次倒退**。
  v030 已刻意拆干净变量（fp32 排除 bf16 混淆、无 pick 排除集成混淆、用已验证的 ck84），仍倒退最狠。
  **不再尝试仅用 dev 数据微调。**
- `ingest` | **v029 fun-asr 第三仲裁源 → 封死**。dev 17.200%，回归 **+1.558 点**；换 17 段却 del+149 / ins+148。
  死因：fun-asr 整段转写文本与 MOSS 段边界不匹配。与 X9、D7.1 同向。3way 代码存 `stash@{0}`。
- `ingest` | **CSSD 短窗+严聚类 → 证伪**。seg_dur=1.0 上扫 merge_thr，提高阈值反而涨 falarm（18→30），
  missed 锁死在 49；最佳档 22.950% 仍输默认 22.754%。cssd-paradigm 留的那条「真信号」不成立。
- `ingest` | **归属轴换 CAM++ 判官 → 证伪**。dev 16.712%，比 v027 的 ERes2NetV2 版差 **1.095 点**。
  CAM++ SV 的 EER（6.78%）本就劣于 ERes2NetV2（6.14%），短窗上更不稳。
  **归属轴瓶颈不在判官选择，在更深的机制。**
- `ingest` | **D9 2D 扫参（seg_dur × merge_thr）→ 关卡拦下**。最优组合 normalize 后 dev 15.339%，
  看似破 v028 的 15.643%，但 **top-3 段占净收益 261%**，剔除后 **−0.489 点**，heavy_tail_gate FAIL。
  这是关卡**第一次在提交前成功拦截**一个「dev 更好但会翻车」的版本。
- `lint` | **补录对账（本次）**：v030 已提交但 **ledger 无行、产物未归档**，wiki 水位停在 v026。
  三处均已补齐；v030 归档为 `submit/archive/submission_v030_20260809.json`（`b80b1e0f…`，SHA256 已校验）。
- `select` | 当前最佳 v028 = 0.15573，距榜首 **0.550 点**。文本轴多源、微调、归属轴判官、
  CSSD 短窗、2D 扫参**五条同期封死**。下一步方向待定。

### 2026-08-06 ～ 08-08（补录：v020 / v024 / v026）
- `ingest` | **v020**（dev 16.563%）：pick_ensemble 当前 H+D 规则重建。同日 480 配置穷举证明
  **该规则族内无更优点**，选择轴（D5.1）确定性耗尽。线上 0.16352。
- `insight` | **[[arbitration-signal-must-be-acoustic]]**：一天 17 次仲裁实验，16 次失败的共同形状是
  **信号全部文本派生**（长度/自一致/模型自信度/LM 流畅度），看不见「文字是否与实际说出的话相符」。
  LM 打分方向甚至是反的（FireRed 的错误是**流畅的错误**）。唯一成功的是**音频条件**的
  teacher forcing 声学似然，区分度 +1.195 且方向正确。
- `ingest` | **v024**（dev 16.090% → **线上 0.15903，新最佳**）：段级声学仲裁，margin=0.05。
  实测迁移率 **95%**，远高于此前假设的架构级 65%。
- `ingest` | 同期两条否定：**n-best 候选扩张失败**（oracle 扩 1.093 但捕获退 0.186 —— 候选越多越难选）；
  **裁判偏置担忧不成立**（FireRed 单独已是最佳裁判，MOSS co-judge 更差）。
- `ingest` | **v026**（dev **15.920%** → **线上 0.15756，新最佳**）：仲裁改**词级**粒度，段内按差异块逐块二选一。
  与 n-best 的分界线：**判别负担没有放大**——这解释了为何 n-best 失败而词级成功。
  train82 16.175 / holdout24 15.047，**holdout 改善更大**（非过拟合）。
- `select` | 主线 = 文本轴声学仲裁（v024→v026）。归属轴（说话人）未受益，仍是下一个待开的轴。
- `ledger` | **v026 线上 = 0.15756，新最佳**。破 v024 的 0.15903 共 **0.00147**，距榜首 0.15023 差 **0.733 点**。
  归档 `submit/archive/submission_v026_20260808.json`（SHA256 与源文件逐字节一致）。
- `insight` | **文本轴声学仲裁的迁移率被第二次独立测到**：v020→v024 是 95%（dev −0.473 → 线上 −0.449），
  v024→v026 是 **86.5%**（dev −0.170 → 线上 −0.147）。两次同量级，且都远高于选择规则族的 **6.5–15%**。
  → **改动类型决定迁移率**：动文本内容的改动几乎全额兑现，动「选哪个源」的规则大部分蒸发。
  这条应写进 [[dev-online-transfer]]，作为按改动类型分层的第三个数据点。
- `lint` | 🔴 **校准超期 8/3**（`last-synced-calibration` = v011，其后 v012/v013/v014/v017b/v018/v020/v024/v026）。
  建议下次线上回执后先跑 calibrate 重分层，再继续迭代。

### 2026-08-04（v018 补录 + 校准超期告警）
- `ledger` | **v018 线上 = 0.1634，新最佳**。破 v017b 的 0.16377 共 **0.00037**，
  距榜首 0.15023 差 **1.317 点**。归档 `submit/archive/submission_v018_20260804.json`（已在档）。
  **实测口径**：v017b 基础上 **33 段由 MOSS 源换成 CAM 源**，其中 **23 段强制重聚类为 5 说话人**
  （段边界与文本同 base，记录数逐段一致）。**触发规则未确证**，见下。
- `lint` | **补录对账：v018 在文档层曾 100% 零记录**。接手时水位停在 v017b，实际磁盘已到 v018。
  缺的不是一处而是全部 —— **无 commit、无 `logs/2026-08-04.md`、无 SCORES 行、无 ledger 行、
  无 dev 评测**，唯一存在证据是三件 untracked 产物。**这是 v014 事故的下一级**：
  v014 是「判断只写进 commit message = 等于没写」，v018 是**连 commit message 都没有**。
  已补 SCORES 排行榜 + 日志索引、`online_ledger.md` 提交行、`logs/2026-08-04.md`、本页。
- `lint` | **校准超期 5/3**。`last-synced-calibration` 停在 v011，其后已提交 v012/v013/v014/
  v017b/v018 共 **5 次**，触发线是 3 —— **超了 2 版才被发现**（v017b 那轮就该触发）。
  `submit/archive/` 八份提交物齐全，重分层输入不缺，**可立即开跑**。本轮未做校准，水位保持 v011。
- `insight` | **「产物在、代码不在」= 最优版本不可复现**。08-04 当天没有任何 `.py` 被修改，
  生成 v018 的脚本在全仓找不到。这是第三次同型事故（v001 代码丢失 / v007 脚本只活在 `/tmp`），
  但**前两次丢的是历史版本，这次丢的是当前最优版本**。→ `memory/open-questions.md` Q5。
- `insight` | **反推能确定「发生了什么」，不能确定「规则是什么」**。按 (CAM 人数, MOSS 人数)
  分格统计触发/未触发：触发集确实集中在 CAM 人数偏高（5/6 占 27/33）且 CAM ≥ MOSS 的格子，
  方向与「CAM×MOSS 人数组合择优」一致；**但同一格子同时出现在两边**
  （CAM=5/MOSS=4 触发 5 / 未触发 11；CAM=5/MOSS=5 触发 12 / 未触发 6）
  → **至少还有一个观测不到的判据**。**故本次全部按实测口径记录，不把该规则写成事实。**
- `lint` | **附带发现，未处理**：`submission_v017b.json` 与 `submission_v010.json` **逐字节相同**
  （同 `34d48178…` / 818677 字节），而文档记它们是两条不同规则；且 ledger 称 v017b 相对 v011
  「test 仅 7 段换源」，**实测 88 段不同**。v010 从未提交 → 没白耗机会。
  **本次刻意未改 v017b 任何既有记录**（超范围），挂账 `memory/open-questions.md` Q6。
- `select` | 下一步不是新方向，是**还债**：① 抢救并 commit v018 生成脚本（最高优先级）；
  ② 跑 calibrate 重分层；③ 补 v018 dev 评测；④ 查 Q6。

### 2026-08-03（D6.5 SoulX 30B 证伪 + v017b 补录）
- `lint` | **补录对账**。接手时水位停在 D6.4+v014，实际已到 v017b。发现两处漂移：
  ① **SCORES.md 完全没有 v017b 行**（只有 `online_ledger.md:38` 记了），已补；
  ② **`.gitignore` 里的 `logs/` 已被 commit `4cc1a00` 提交** —— 当天新写的
  `logs/2026-08-03.md` 直接不入库（`git status` 里根本看不到）。该问题 08-02 已发现并
  警告过、但没当场修，一天后就造成实际损失。已删掉那一行。
  **教训：发现的问题当场就修，"先记着回头再说" 等于没发现。**
- `ledger` | **v017b 线上 = 0.16377**（v011 + CAM++≥6人则换回 MOSS）。破 v011 的 0.16379
  仅 **0.00002** —— test 只有 7 段触发换源，几乎没赚到。成为当前最佳，距榜首 1.354 点。
- `ingest` | **D6.5 SoulX-Transcriber 30B → 封死**。全量 dev **31.44%**，比 MOSS 0.9B
  差 13 点。见 `logs/2026-08-03.md`。
- `insight` | **域墙的最强反例也倒了**：SoulX 是迄今条件最好的候选 —— 端到端 SAT
  （本赛唯一有效范式）、**参数量 33 倍**、AliMeeting DER 5.39 打赢 Gemini、Apache-2.0
  合规。仍然输 13 点，失败方式仍是塌缩（009 把 5 人判成 2 人，全量 6 人段为 0）。
  → **「换个更大更强的模型」这条路已被最有利的样本证伪**，不要再按 benchmark 选模型。
- `insight` | **prompt 救不了塌缩**（五个单变量实验，本项目对该问题最系统的一次检验）。
  段粒度**完全不受 prompt 控制**（明确要求「每次换人就切段」，段数 1015→1013 纹丝不动，
  与 X11 同源）；说话人数提示是唯一有效杠杆（31.44→30.45，missed_spk 100→87）但只有
  边际效果，6 人段仍为 0。→ **别再为 prompt 调参花 GPU 钱。**
- `insight` | **第三源增量连续两次不达标**：MOSS+CAM 上界 14.73% → 加 ERes2NetV2 14.31%
  （−0.42）→ 加 SoulX **14.52%（−0.21）**。两个「错误结构不同」的源都推不动上界，
  再次印证 D6.4：错误结构不同是必要条件、不是充分条件。
- `insight` | **诊断手法沉淀**：`ins`/`del` **成对出现而 `sub` 极低** = 归属错（塌缩或
  时间轴错位），不是识别错；反之 `sub` 高才是文本问题。这个签名让根因定位从「逐段听
  音频」变成「看一眼误差分解」。

### 2026-08-02（D6.4 换嵌入模型 + 外部语料调研）
- `lint` | **补录对账**。接手时水位停在 v012、实际已到 v014。发现 **v014 在文档层零记录** —— commit `e60dec2` 的 message 称「已在 SCORES/ledger 标注」，但 `grep -rni v014 *.md` 无任何命中。已亲自复验 `shasum -a 256`：`submission_v013.json` 与 `submission_v014.json` 同为 `fc03970c...`、同为 983149 字节，确系重复产物 → 已补入 SCORES 排行榜。**但补录晚了一步：v014 已于当日提交，线上 0.16872，与 v013 逐位相同，白耗一次机会。**
  **教训：判断只写进 commit message = 等于没写**；提交决策前先 `shasum -a 256` 比对上一版提交物。（CLAUDE.md 已有「HANDOFF stale-by-default」条款，本次证明 commit body 同样适用。）
- `insight` | **线上评测是确定性的**（v014 副产物，此前从未验证）。逐字节相同的提交物 → 线上分逐位相同（0.16872）。含义：线上分之间的任何差异都是真实信号，不必为「评测抖动」留裕度；重复提交同一产物也不可能靠运气刷出更好的分。
- `ingest` | **D6.4 ERes2NetV2 换嵌入模型 → 封死**。全量 dev 扫 4 档 merge_thr（0.60/0.70/0.82/0.92 → 32.80/27.27/31.68/36.78%），最优档仍输 CAM++ 22.75% 达 4.52 点。作为第三源，按段择优上界仅 14.73%→14.31%（+0.42 点，e2v2 只赢 7 段）。见 `logs/2026-08-02.md`。
- `insight` | **域墙比原以为的更宽**：原页推论「优先『嵌入+聚类』范式」被 D6.4 部分推翻 —— **同范式内换模型也不迁移**。ERes2NetV2 benchmark 更强（CNCeleb EER 6.14% vs 6.78%）、参数量 2.5 倍、同一 pipeline 同一聚类、各自最优工作点，仍输 4.5 点。已追加进 `wiki/insights/neural-diarizer-domain-wall.md`。
- `insight` | **人数判对 ≠ 时间轴切对**（本项目首次分离观测到这两个能力）。ERes2NetV2 在 thr=0.82 时人数分布比 CAM++ **更接近 ref**（4人段 32 vs 28、6人段 4 vs 2），`missed_spk` 55 优于 CAM++ 的 61 —— 它确实更能找到人，但 ins/del 暴涨到 1992/3035（CAM++ 1142/2185）。→ **说话人数可用于淘汰、不可用于录取**，原域墙页「人数是极便宜的早期筛子」需按方向限定。
- `insight` | **错误结构不同是加源的必要条件、不是充分条件**。e2v2 的错误结构（找到人但切不准）确与 CAM++ 不同，符合 [[ensemble-oracle-bound]] 的加源推论，但上界只降 0.42 点 —— 还要求该源在它赢的段上**赢得够多**，否则择优规则的误触发代价会吃掉全部增益。
- `select` | **路线1（外部语料微调）调研完成，未动手**。硬门槛 = 必须同时有转写 + 说话人时间戳。结论：**无任何公开集同时满足「有转写 + 3-6 人 + 近场自由对话」** —— 域最贴的 MSDWild（爬公开视频、轮次密集、重叠率最高）只有 RTTM 无文本；有文本的 AISHELL-4 / AliMeeting / AISHELL-5 / MagicData-RAMC / SeniorTalk 要么会议远场、要么车内、要么只有 2 人。剩两条未验证路径：A 仿真拼接（唯一能精确匹配人数分布）、B MSDWild + fun-asr 伪转写。两者均受 [[dev-online-transfer]] 约束。
- `rescue` | `src/oracle_pick.py` 入库（按段择优上界诊断）。此前 15.05% / 12.40% 两次上界测算的脚本从未入库，本次补缺口。入库版复现 MOSS+CAM++ = 14.73%，与当年记录的 15.05% 吻合。

### 2026-08-01（D5.3 微调）
- `ingest` | **v012 = 微调 MOSS 0.9B + 按段择优**。holdout 25 段（同机 bf16）：基座 20.535% → 微调 18.102% → +集成 **16.196%**；同口径 v011 为 18.562% → **降 2.37 点**。del 387→297→266，missed_spk 12→9→8。提交物 `baseline/submission_v012.json`（5909 条/394 session）。见 `logs/2026-08-01_finetune.md`。
- `insight` | **微调与按段择优近乎完美可加**（−2.43 与 −1.97，合计 −4.34 ≈ 之和 −4.40）→ 两者打的是不同失败模式：微调改模型行为、集成捞回剩余失败段。**互补的改动可以叠乘收益，不必二选一。**
- `insight` | **中途 checkpoint 不可跨 run 比较**：12 轮 run 的第 6/8 轮（cosine 学习率仍在高位）看着极差 23%，与 4 轮 run 完成退火的第 4 轮（18.10%）不是一回事。只有同样跑完退火的最终 checkpoint 才可比。差点据此误判「微调超 4 轮就崩」。
- `insight` | **迁移率理论的效力边界**：按等段数比例（前 12% session）对齐后，架构改动 84.8% / 微调 85.6% / 选择规则 **162.8%** —— **集中度本身不能单独预测迁移率**（架构改动同样集中却迁移 65%）。v011 的真正异常是占比 >100%：净收益全靠少数大赢抵消大面积亏损。**跨样本量比集中度必须按段数比例对齐**（25 段的前 3 段占 12%，106 段的前 3 段仅 2.8%，直接比会得出相反结论）。本轮据此连续两次误判（先过乐观、后过悲观），已记入日志。
- `insight` | **三个工程坑**：① HF **Xet 后端 401**（学术加速代理下认证失败）→ `HF_HUB_DISABLE_XET=1`；上台机器「2B 下载卡死」大概率同因，当时误判为网络问题。② **AutoProcessor 静默降级**：Trainer 存的 checkpoint 缺 `preprocessor_config.json`，`AutoProcessor` 返回 Qwen2Tokenizer 且不报错，推理中途才炸；首次验证只查「没抛异常」导致 4 个 checkpoint 空跑 → 已改为强制核实类型。③ **macOS AppleDouble 文件** `._001.wav` 混入 tar，被当音频解码崩溃；dev 因 `--sessions` 过滤未暴露，test 全集才炸。
- `select` | 下一步 = **扩训练数据**。dev 仅 56 分钟音频；赛题 §149 允许外部开源数据（需说明来源/规模/处理方式），候选 AliMeeting / AISHELL-4 / MagicData-RAMC。

### 2026-08-01（校准重分层）
- `ledger` | **v011 线上 = 0.16379**。归档 `submit/archive/submission_v011_20260801.json`。破 v007（0.16593）**仅 0.214 点**，而 dev 上是 1.41 点 → **迁移率仅 15%**。距榜首 0.15023 差 **1.36 点**。距校准 3/3 **已触发并完成重分层**。
- `insight` | **落 `wiki/insights/dev-online-transfer.md`：dev→线上迁移率由改动类型决定**。架构级换模型 ≈**65%**（v002→v007：dev −4.40 → 线上 −2.874）；dev 上拟合的选择/后处理规则 ≈**15%**（v007→v011）。偏移同时系统性收窄：−3.32 → −1.80 → −0.60。
- `insight` | **v011 迁移差不是过拟合**——test 触发率 31.2% 与 dev 31.1% 完全吻合，规则触发得对，只是触发后没赚到钱。**真因是 dev 收益重尾 + 样本太小**：dev 净省 274 错误中前 3 段（016/047/030）占 **85%**；剔除这 3 段后 dev 收益 −0.21 点 ≈ **实际线上 −0.214 点**（吻合到小数点后两位）。dev 106 段恰含 3 个 MOSS 灾难段，test 394 段无同等密度。→ **可操作规则：估线上收益看「剔除 top-3 段后的 dev 收益」，不看全 dev 收益。**
- `select` | **D5.1 轴判定结构性耗尽**（走可达性探针门）。oracle 上界 14.73% 同样高度集中（前 10 段占 77.8%）；剩余 2.25 点按 15% 迁移 → 线上 0.34 点 → 16.04%，**距榜首仍差 1.02 点**。**即便完美 oracle 选择器也够不到**。→ D5.1 收官于 v011，不再主攻。
- `select` | **主攻改为 D5.3 微调 MOSS 0.9B**。依据：闭合 1.36 点需约 2.1 点 dev 级改进，按迁移率只有架构级改动（65%）够得着；微调直击已定位的病根（少判说话人的 domain mismatch），且基础设施已就绪（GPU 机环境 + `src/prep_finetune_data.py` + train81/holdout25）。

### 2026-08-01（续挖第二轮）
- `ingest` | **v011 = D5.1 双症状并集，dev 16.98%**（破 v007 1.41 点、破 v010 0.46 点，**train/holdout/全dev 三切分全胜**，dev/test 触发率 31.1%/31.2%）。规则：`CAM++≥3人` 且（`MOSS≤2人` 或 `MOSS切换率≤0.10`）。提交物 `baseline/submission_v011.json`。
- `insight` | **同一故障有多个独立症状，单症状规则会结构性漏检**。MOSS「并人」的症状①是自称人数少、症状②是**说话人切换率塌陷**。症状②不依赖它自称几人 → 能抓到「真 5 人但 MOSS 判 3 人」，而症状①**结构上够不着**这类。dev 触发 33 段里 23 段只靠症状②。**并集不是调参凑出来的，是覆盖面互补。**
- `insight` | **前置检查全过 ≠ 能泛化**。R3（只用症状②）通过了：嵌套 CV 5 折中 4 折选中它、阈值扫描平滑最优（非尖峰）、逐折最坏值 +0.18 优于 v010 的 +2.69、dev/test 触发率吻合——**仍然在 holdout 上输给 v010 0.81 点**。逐段诊断才发现原因：它漏掉 091 这一段（省 64 个错误）。**诊断到 session 级才看得见互补性，聚合指标看不见。**
- `insight` | **holdout 用一次少一次**。这轮改为 train 内部 5 折嵌套 CV 选规则（评估「搜索流程」而非某条规则），选定后只上 holdout 一次。325 条候选的嵌套 CV 泛化收益仅 −0.38 点，远低于单条规则在 train 上的表观收益，印证了选择偏差的量级。
- `select` | D5.1 残余 2.25 点（oracle 14.73%）。**session 级聚合特征已试穿**（8 类特征 × 325 条候选）。下一步该换粒度：两系统**时间轴级**分歧度，或引入第三个源抬高 oracle 上界。

### 2026-08-01
- `ingest` | **v010 = D5.1 按段择优兑现**。规则 **R1a：MOSS 判≤2人 且 CAM++ 判≥3人 → 改用 CAM++**。dev **18.39% → 17.44%（−0.95 点）**，holdout 25 段独立验收 **−1.40 点**，ins/del/sub/missed 全降（727/1528/1136/44），仅 falarm +2，无回归。提交物 `baseline/submission_v010.json`（4592 条/394 session）。脚本 `src/pick_ensemble.py`（已验证复现一致）。
- `insight` | **holdout 关卡救了一次**：R2（MOSS 段数≤5 → CAM++）在 train 上 −0.44 点，若直接采信就上线了；holdout 上 **+0.07 点**，判定过拟合丢弃。防过拟合切分不是形式主义。
- `insight` | **单信号启发式要看跨集触发率，不能只看分数**。R1 单条件 dev 触发 17.9% 但 test 26.4%（分布漂移）；dev 拆解显示触发段里 47% 是 MOSS 判对的真 2 人段被换差，净赢全靠「漏判 3 人」那一支。加「CAM++ 独立佐证 ≥3 人」后误伤滤掉，dev/test 触发率回到 9.4%/8.9%，且三切分分数全优。**要求两个独立系统同时表态 = 廉价的置信度代理**。
- `ingest` | **D5.2 证伪封死**：`MOSS-Transcribe-preview-2B` 是**纯英文 ASR、无 diarization**（language: en，训练集全英文，pipeline_tag: ASR，架构也不同）。v009 花整轮攻的下载阻塞是伪问题——实测可下（HF 3MB/s、hf-mirror 4MB/s，20 分钟），但下下来也用不了。MOSS Pro 闭源 API，按 FAQ#7 亦不可用。**教训：换模型前先核 model card 的 language/pipeline_tag/训练集，别只看参数量**。
- `ingest` | **D5.3 微调基础设施就绪未执行**。GPU 机（3090 共用）独立 conda 环境建好（torch 2.9.1+cu128 + MOSS + 权重），dev 音频与 train81/holdout25 JSONL 已上传校验。用户选择先走 D5.1。**实测：共用机上 MOSS 推理比本地 M4 慢 6 倍**（自回归每 token 的 CPU 开销被争抢放大，load avg 36；GPU 本身健康 bf16 27.5 TFLOPS）→ 若做微调，GPU 只承担训练（teacher-forcing 不吃这个亏），推理评测留本地。

### 2026-07-31
- `lint` | **补录对账**。发现三处漂移并已修：① `last-synced-version` 停在 v002-线上、实际已到 v009 → 补 v007/v008/v009 时间线；② SCORES 排行榜缺 v008/v009 行 → 已补，并写入新榜首 0.15023（旧记录 16.7% 已过时）；③ `memory/directions.md` 停在 X9，整条 MOSS family（D5/D5.1/D5.2）与 X10-X12 缺失 → 已补。
- `lint` | **未修的问题（需人工决策）**：方向数已达 21，超 CLAUDE.md 惰性升级阈值（≥10 拆 `wiki/ideas/` 单页），本轮未拆——结构性重构留给人工确认。
- `ingest` | **v007 生成脚本抢救入库**。此前 MOSS 全部推理脚本只存在于 `/tmp`（`moss_n.py`/`moss_test.py`/`moss_gpu.py` 等 7 个），`baseline/src/` 零 MOSS 代码，`/tmp` 一清即失去 v007 复现能力——与 v001 丢代码同类事故。已合并为 `baseline/src/moss_sat.py`（CLI 化 model / wav_dir / out_dir / max_new_tokens / prompt-preset）。**验证：`--sessions 001` 复跑与 `output/v007_moss/001.seglst.json` 逐字节一致**（11 段 2 人，17s/段 M4 CPU），ruff 通过。三个 v008 证伪 prompt 作为 preset 保留供复验。

### 2026-07-30
- `ingest` | **v009 GPU 服务器部署 MOSS（未完成）**（见 `logs/2026-07-30_gpu.md`）。RTX 4090 环境就绪，0.9B 跑通 001 与本地一致。**踩坑**：服务器默认 ascii 编码 → 必须 `sys.stdout.reconfigure(encoding='utf-8')` + `write_text(encoding='utf-8')`，否则静默写出 0 字节文件（已固化进 `moss_sat.py`）。**两处阻塞**：2B 权重 4GB 全下载路径卡死（服务器 hf_hub / 本地 curl+wget / 本地 snapshot_download 均失败）；SSH 频繁触发 fail2ban 封禁。无新分数。
- `select` | D5.2（换 2B/Pro）是**基础设施阻塞非证伪**——换镜像源（hf-mirror / modelscope）是未试穿的廉价动作，不该按死路处理。

### 2026-07-29
- `ingest` | **v008 MOSS 提升探针，三条全证伪**（见 `logs/2026-07-29_v008.md`）。prompt 细切段无效（49.94% vs 49.81%）；prompt 强调说话人**破坏输出格式**（6 段中 5 段 0 输出）→ X11；CAM++ 说话人覆盖 MOSS 文本 = 27.79%，差 9.4 点 → X12，且推翻了「本地 diar 优于模型自带 diar」的旧前提（那只对 fun-asr 成立）。MOSS del=1620 根因查清 = 少判 1-2 个说话人，非 token 截断。
- `ingest` | **唯一正向发现：MOSS+CAM++ 按段择优上界 15.05%**（互补性 67:37:2），几乎持平新榜首 15.02% → 落 `wiki/insights/ensemble-oracle-bound.md`。但 dev ref 引导，test 无 ref 难落地。
- `select` | 主线维持 v007。下一候选 = D5.1 无 ref 启发式择优。**注意 X6 教训：增益来自「择优」不是「voting」**，别重走 DOVER。
- `ledger` | **v007 线上提交 → tcpWER=0.16593（16.59%）**。归档 `submit/archive/submission_v007_20260728.json`。dev 18.39% → 线上 16.59%（dev 保守 1.8 点，与 v002 趋势一致）。**榜首已刷新为 0.15023**，距目标差 1.57 点。距校准 2/3。

### 2026-07-28
- `ingest` | **v007 MOSS-Transcribe-Diarize 0.9B 端到端 SAT = 新主线**（见 `logs/2026-07-28_moss.md`）。dev **18.39%**，破 v002 4.40 点，ins/del/sub + missed/falarm **全降无回归**。Apache-2.0 合规，M4 CPU ~16s/段。**架构切换点**：从「fun-asr 文本 + 本地 diar 重打标签」两段式 → 端到端 SAT。
- `insight` | **X10 与 D5 的分界 = 时间戳是否被专门训练过**。qwen-omni（通用多模态 LLM）猜时间戳 → 001 音频 27.8s 只给到 14.56s，封死；MOSS（专门训练 SAT）时间戳准，覆盖 0.08-28.55s 全程。这条分界可用于筛选后续候选模型。

### 2026-07-29
- `ledger` | **v007 线上提交 → tcpWER=0.16593（16.59%）**。归档 `submit/archive/submission_v007_20260728.json`。**线上 16.59% 低于榜首 16.7%**！dev 18.39% → 线上 16.59%（dev 保守 1.8 点，和 v002 趋势一致）。距 goal <16% 只差 0.59 点线上。距校准 2/3。

### 2026-07-28
- `ingest` | **v006 qwen-omni-turbo 端到端 SAT 探针**（见 `logs/2026-07-28_omni.md`）。专属实例有 qwen-omni-turbo（不在 list 但能调），加 `X-DashScope-OssResourceResolve: enable` header 能读 oss url。omni 能输出 `[spkN] 时间 文本` 端到端 SAT。**但时间戳是 LLM 编的严重不准**（001 音频 27.8s，omni 给到 14.56s）。001 直接 113.99%，混合 fun-asr 时间戳仍 52.45%（远差于 v002 6.99%）。**v006 封死**（X10）。发现有价值：专属实例 omni 可调 + OssResourceResolve 机制可复用。下一步：本地跑 MOSS-Transcribe-Diarize 0.9B（专门训练 SAT，时间戳应准）。
- `query` | 百烈 SAT 调研（见 `logs/2026-07-28_bailian_sat.md`）。百烈有 qwen-audio-turbo/omni 但无 MOSS/TagSpeech。MOSS 要本地跑。
- `ingest` | **2026 SOTA 调研**（deep-research 102 agents）。落 `wiki/insights/diarization-sota-2026.md`。两个未试 2026 端到端 SAT：MOSS-Transcribe-Diarize 0.9B（Apache-2.0，中文 cpCER 22.17 Alimeeting）/ TagSpeech（ACL 2026，HF 权重在）。JEDIS-LLM 无开源不可用。DM-ASR 论文自述"LLM 端到端不如强 diar 前端"强化当前架构。
- `query` | 云端 ASR 调研（见 `logs/2026-07-28_asr_survey.md`）。公共百烈有 paraformer-v2/sensevoice-v1/paraformer-v1（task 接受）但当前配置用不了：专属实例只部署 fun-asr，公共百烈读不了专属 oss（DownloadFailed），公共 ASR 要公网 https url 不收 oss/base64。要试需配公网 OSS 桶或百烈数据集托管。paraformer-v2 是 2024 新版（≠本地 paraformer-zh 旧版），可能文本质量比 fun-asr 好，是 D4 文本端唯一可能突破的路。

### 2026-07-27
- `ingest` | **文本端三路全证伪**（见 `logs/2026-07-27_d4.md`）。LLM 后处理（自由 del 83→94 增错 / 保守 del 82 净收益 0.27）→ LLM 无音频靠猜，证伪。paraformer-zh 文本质量比 fun-asr 差（094: 同学→父亲、第一晚→地外）→ fun-asr 是文本天花板，换 ASR 证伪。pyannote community-1 gated=auto 要 HF token，阻塞。
- `select` | 唯一剩余自主路 = 按段择优启发式（test 无 ref 版），上界 21.37% 离 16% 差更远，但能破 v002。要破 16% 必须更多源/更强模型，全阻塞（EEND-TA 权重未发布 / pyannote 要 token）。需用户给 HF token 或接受停在 v002。
- `ingest` | **D2 证伪 + 所有便宜档撞墙**（见 `logs/2026-07-27_d2.md`）。给 fun-asr 传 oracle speaker_count 只降 0.8 点（46.60%→45.79%）→ fun-asr diarization 失败是算法本身弱非参数问题。D4 文本规则后处理无空间（hyp 重复字比 ref 还少，删了增 del）。
- `select` | **当前约束下 goal <16% 自主不可达**。所有便宜档试穿，expensive 档全阻塞（换 ASR 禁/LLM 锁/EEND-TA 权重未发布/pyannote 要 HF token）。需用户介入解锁：LLM 合规确认 / HF token / 本地 paraformer / 或接受停在 v002。
- `ingest` | **DOVER-Lap 集成失效 + 择优上界 21.37% 落 insight**（见 `logs/2026-07-27_dover.md` + `wiki/insights/ensemble-oracle-bound.md`）。DOVER rank-weight=27.63% = CAM++默认（Sortformer 零贡献），等权=28.98%（Sortformer 主导），voting 机制不匹配粒度不同的系统。**按段择优上界 21.37%**（破 v002 1.42 点，但 CAM++64/SF40/DV2 → 增益来自择优非 voting）。距 16% 差 5.4 点，两源不够，需文本端协同。
- `select` | 转文本端（del=2473 占 49% 最大块，fun-asr 漏字 13%）。DOVER voting 封死。主线维持 v002。
- `ingest` | **Sortformer v2 探针完成**（见 `logs/2026-07-27.md`）。新建 nemo_venv（torch 2.13 + nemo 2.7.3，patch 2 处版本不匹配），写 `src/diarize_nemo.py`。全量 dev **29.70%**，输 v002(22.79%) 6.9 点。missed=87/falarm=2（保守少判，和 DiariZen 同病，069:6人判3人）。推理快 36 倍（0.9s/段）。
- `ingest` | **per-reco 互补性强**：Sortformer 赢 42 段（均赢 15.2 点）/ CAM++ 赢 37 段（均赢 24.2 点）/ 平 27 段。各自大赢段（095/075/021 vs 019/053/047）。
- `select` | **v004 单独封死**（全量输 6.9 点）。**v005 候选 = DOVER-Lap 集成**（CAM++ + Sortformer，42+37 互补段，deep-research finding 4 冠军都是集成）。主线维持 v002=22.79%（线上 19.47%）。

### 2026-07-26
- `ingest` | **cVBx 扫参探针完成**（见 `logs/2026-07-26_cvbx.md`）。修改 diarize_dz.py 加 `--fa`/`--fb` 暴露 VBx 超参，5 段 overlap 密集段扫 4 组：默认 43.14% → Fa0.3/Fb1.0 32.18% → Fa0.5/Fb1.2 **30.22%**（饱和）。**caveat 6 部分成立**：17.5 点反差缩到 4.6 点，但仍输 CAM++ 25.61%。**X4 死因再修正**：配置调优后仍输 CAM++ + 许可证堵死。CAM++ 主线 merge_thr 0.68/0.70 最优（22.82/22.79%），已挖到头。
- `ingest` | **deep-research 完成**（103 agents / 21 源 / 102 claims / 19 confirmed / 6 refuted）。落 `wiki/insights/diarization-sota-landscape-2025.md`。
  - **X4 封死修正**：v003 用 DiariZen 默认 AHC（次优），SOTA 要 cVBx（降 1.5 点 DER）。17.5 点反差可能是配置 artifact 非模型失败。但 DiariZen CC BY-NC 许可证堵死路线 → X4 维持封死，死因改为"许可证不合规"。
  - **新候选排序**：① EEND-TA（M4 最可行 13.3M/460x RT，但权重发布+许可未确认，open question）② Sortformer v2（中文 overlap 最强 7.0%，CC BY 可商用，但 7.0% 可能不复现+ARM 未知）③ pyannote community-1（MIT，同范式升级）④ MISP 2025 hybrid（cost 最贵）。
  - **硬约束**：DER ≠ tcpWER（collar/单位不同），所有候选必须本赛 dev 实测，不能凭 benchmark 排序。
  - `select` | 先查 EEND-TA 权重发布状态+许可证 → 决定是否进候选池。主线维持 D3 v002=22.79%。
- `ledger` | v002 线上提交 → tcpWER=**0.19467**。归档 `submit/archive/submission_v002_20260726.json`。**线上 < dev（22.79%）3.3 点** → dev 是保守下界，本地涨线上大概率涨。距榜首 16.7% 还差 ~2.8 点。距校准 1/3。
- `ingest` | 初始化 wiki 骨架。把 `memory/directions.md` 现有 D1/D2/D3/D3.1/X1/X2/X3 判断迁入 `wiki/index.md` 分组。方向数 <10，暂不拆单页（按 AGENTS.md 惰性升级条款）。
- `select` | 主线仍为 D3。依据：v002=22.79% 是当前真实最优（seed=0 可复现），距 oracle 上界 15.32% 还有 7.5 点空间，瓶颈全在分离质量（SCORES.md v001 诊断：说话人占 13/26 点）。
- `select` | 下一候选 = D3-DiariZen。依据：CAM++ 段级映射会吞短插话与 overlap（实测 dev 001 的 13.88-14.34 短插话被完全漏掉），dev 83/106 段含跨说话人 overlap，EEND powerset 范式原生支持重叠。
- `query` | D3-DiariZen 查重：新方向，wiki 无前例。决定小样本探针验证。
- `ingest` | v003-probe1（dz 简单 2 人段 001-003）：tcpWER=15.94%，与 v000 同分，比 CAM++ 默认 14.90% 差 1 点。missed/falarm=0/0。判定：简单段无优势，但样本非 overlap 段，不足以判死。
- `ingest` | v003-probe2（dz overlap 密集段 009/011/024/069/098，v000 missed_speaker 最高的 5 段）：dz=43.14% vs CAM++ 默认=25.61% vs v000=46.60%。dz 在 overlap 段**比 CAM++ 差 17.5 点**，EEND powerset 未多找回说话人（069 dz=3人 vs ref 6人）。
- `select` | **封死 D3-DiariZen**。依据：简单段无优势 + overlap 段反差 17.5 点 + 推理慢 3-4x。撞 CAM++ 已验证更优这堵死墙。详见 `logs/2026-07-26.md`。
- `select` | 下一候选 = D3.1 调 CAM++ 聚类阈值（`recluster.py --merge-thr` 精细扫 0.65-0.75 区间）/ oversegmentation / 段合并后处理降 del。主线 D3 维持 v002=22.79%。

### 2026-07-25
- `ingest` | v002 落 SCORES。重建本地 diarization（`diarize.py`/`recluster.py`/`apply_diar.py`），seed=0 复现 22.79%。补 `diarize_dz.py`（DiariZen）待评测。
- `ingest` | v001（d066aa3）标"未复现"——commit 只改 directions.md，代码丢失，21.19% 无产物支撑不采信。

### 2026-07-24
- `ingest` | v001 诊断：纯文本 tcpWER=12.89%，说话人占 13.12 点；oracle 上界 15.34%；missed_spk=59 falarm_spk=17。主瓶颈确认 = 说话人分离。
